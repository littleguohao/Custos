# 待办清单

> **范围**：跨目录的待跑/待重跑/待收敛项。与同目录 `CHANGELOG.md` 的分工——
> **本文件记「还没做的事」，版本日志记「已经改了的策略规则」**，互不混放。
>
> 优先级按**「它阻塞了什么」**排，不按工作量：
> P0 = 阻塞其他事或 live 正在依赖 ｜ P1 = 已有结论悬空 ｜ P2 = 新验证 ｜ P3 = 技术债
>
> 最后更新：2026-08-24（数据层解耦收尾：已失效表补注 qlib 接口删除、P3 新增 #63
> TDX 安装目录文件解析器下沉。当前活跃：#60 影子观察（待 owner 拍板）、
> #61 校准回测、#26 剩余子项挂起、#59 剩余机会、#63 解析器下沉）

## P0 · 阻塞项

（当前无 P0。）

## P1 · 重跑（结论悬空）

逐条对应研究单元的 `**重跑口径**` 字段，详见
[`research/README.md`「⚠️ 重跑清单」](governance/research/README.md)。

| # | 单元 | 重跑什么 | 前置 |
|---|---|---|---|

（当前无。）

## P2 · 待跑（新验证）

| # | 事项 | 出处 | 备注 |
|---|---|---|---|
| 65 | **QSX/DKX 共振细化**（R23 初版口径证伪后 owner 指明继续）：更严共振定义候选——反弹幅度门槛、贴线精度、收回速度、缩量配合；**判据必须先预注册再跑**（本仓库纪律），细化方案先过 owner | R23（v0.120）| 待 owner 定细化口径 |

⚠️ 教训（cross-window 复核，已归入 R10）：edge 集中在单一 regime 的方案首轮看起来都很好。

## P3 · 待收敛 / 技术债

| # | 事项 | 出处 |
|---|---|---|
| 59 | **scorer 双形态剩余机会**：s_shape 系（值得但改造面大）、alpha101/pvcorr/low_vol/momentum/reversal_quality/mcap（中等优先）；b1_dual/long_structure/b2/main_rally 黑盒 detector 不适合。优先级低，随回测批次顺带做 | v0.73 审查（①② 已落地 v0.74；③④ 已否决，见已失效表） |
| 63 | **两处直读 TDX 安装目录文件的解析器下沉 datasource**：`holding_sector_mapper.py`（pipeline 层直读 `tdxhy.cfg`/`incon.dat` 解析行业归属）与 `manual_pools.py`（直读 `blocknew.cfg`/`.blk` 自选股板块文件）。本轮数据层解耦只收敛了 mootdx 直调，这两处走本地文件解析、不踩 vendor 白名单钉测，属灰色地带；解析器应下沉 `datasource/local_tdx/`，留作后续 | 2026-08-24 数据层解耦审计 |

## ⚠️ 已失效的行动项（**别照着做**）

旧文档里这些「待跑」已被后续发现推翻或已完成。留着是为了**防止有人照单执行**：

| 原行动项 | 出处 | 为什么失效 |
|---|---|---|
| 「活跃板块（多次上榜）∩ J<13 命中接 1800」 | #26 / v0.77-v0.79 | **2026-08-19 三轮研究证伪**：120 窗 regime 变体曾「过线」（H20 +2.52pp 两半同向），240 错位窗三 horizon 全部前后半翻转（H20：前半 −0.57 / 后半 +2.52）——弱市抗跌、强反弹市滞涨，edge 随 regime 反转抵消（R10/R4 同款单 regime 假象）。`sector_daily_rank` 采集器保留作 #26 回调四项原料，**此假设不接 1800** |
| 「`fetch_pit_financials` 逐期全量台账读写改批量写（~97% IO）」 | #59③ | **owner 2026-08-19 拍板不动（选 C）**：批量补拉是低频运维操作，IO 浪费可接受；逐期落盘的容错语义（一期失败不丢已完成期）与 stdout 逐期数值口径保留 |
| 「东财分页并发拉取」 | #59④ | 改请求时序与限流特征（东财连打会被静默限流，正是 `FetchIncomplete` 防的场景），无法逐位等价钉住，已否决 |
| 「去幸存者偏差：`--data-source qlib`」 | R10 待跑 #3 | **2021 年后一只退市股都没有**，且 `2021_2026` bundle 已弃用（加法调整）⇒ 照做只会引入放大 13~21% 的收益，去偏一点没做到。近期窗口**目前无法去偏**。⚠️ qlib 数据接口已于 2026-08-24（v0.109）整体删除，本条连执行路径都没有了 |
| 「补探止损下界 `pct_03`/`pct_04`」 | R10 待跑 #4 | ✅ 已完成 —— **5% 是崖不是坡**（5%→4% 掉 42%）|
| 「`--exclude-zero-ret` 复跑」 | R3 | ❌ 已证是**错误的修正**（删的是合法样本），已标注勿用 |

## P4 · strategy 梳理查出的问题（2026-08-06）

索引与分类见 [`strategy/README.md`](governance/strategy/README.md)。

| # | 事项 | 性质 |
|---|---|---|
| 26 | **板块信息利用整体优化**：方向已转清理瘦身（v0.80 四项拍板已执行：1800 主线指纹节/cz 板块否决/资金意图板块腿已删）；剩余子项（回调四项切换检查脚本化、主线题材判定重设计）在清理方向下优先级存疑，**下次 owner 提到时再定是否还做** | 剩余子项挂起待 owner 重提（进展见 CHANGELOG v0.77-v0.80） |

## P5 · contracts 梳理查出的问题（2026-08-06）

索引与核查结论见 [`contracts/README.md`](governance/contracts/README.md)。

（当前无。）

## P6 · trades 梳理（2026-08-06）

（当前无——#32 观察期完成，v0.103 已转硬闸。）

## P7 · 因子层（2026-08-06）

| # | 事项 | 性质 |
|---|---|---|
| 61 | **capital_intent 及因子分值校准回测**（status candidate → active 的前提；#62 迁入的 7 个技术分轴因子的 status 升级同批议）：调 `scoring.weights` 一律先回测（同 cap_rules 纪律）；用 `strategy_grid` 因子轴纳入组合寻优。⚠️ R19（2026-08-21）后重新定位：技术分总量对涨幅无预测力 ⇒ 校准的目标函数**不能**是「预测涨幅」，应是「分层行动语义下的盈亏比/胜率组合」；实跑排在 R11 之后 | 待回测（目标函数待 R19 后重定义） |

## 需要 owner 拍板

| # | 事项 | 出处 |
|---|---|---|
| 60 | **plan 信号并入 SIGNAL_ORDER 与否**：v0.83 影子期观察——14:45/盘后两份报告每日输出「计划判定 vs 现行判定」对比（⚠️影子不一致标注）。**连续 5 个交易日**影子对比后 owner 拍板是否把 plan 信号（plan_stop_breach/plan_tp_scale_out）并入 `b1_holding_state.SIGNAL_ORDER` 转正式判定；切换本身是**独立提交**，不动其他字段。注意：影子期存量持仓多数无计划（早于机制落地），对比样本只含新建仓票。**判读注记（v0.86 review 发现的两个结构性事实）**：① 两份报告的「现行判定」列口径不同——review_core §2.1 比 `classify()` 输出（含 B1 短路/硬风险层），final_close_review §5 比 `b1 final_priority`，同一持仓可能一边「一致」一边「⚠️」，对照时以各自口径为准；② `source=default` 的计划止损=entry×0.93，恰与 live −7% P1 减仓线重合而影子判 P0 ⇒ 每张 default 计划到 −7% **必然**亮「⚠️影子不一致」——这是设计使然的噪音不是信号，统计一致性时应剔除 | 「因子×止盈×止损」架构计划 Phase C（v0.83） |
| 64 | **rsi_deep（RSI 深水）进 live 与否**：R21 证据已齐——画像富集四臂全稳（R20）+ 进场 gate 跨窗通过（四档出场压基底，margin +26~+42pp，Wilson 与基底不重叠）。拍板点：① `rsi_state` 因子 status 升级（untested→candidate/active）；② 1800 接线方式——RD 标注升级进分层/门内提醒/还是新 gate 过滤；③ 若进，止损档位联动（跨窗显示 deep 配 12% 宽止损最优，与 live 现行 −7%/−10% 口径不同，需走 EXIT_RULES 配置而非硬编码） | R20/R21（v0.104-v0.111） |
| 66 | **V2/V3 重构打分进 live 路径**：R22 放宽候选（双窗 C1/C2/C4 + 放宽 C3 全过）——拍板点：① 先跑独立窗口（2019 前）复核再谈接线；② V2 进 strategy_grid 因子轴需先做成 backtest scorer；③ 若接 1800，与 #64 rsi_deep 同批议（V2 已含深水腿）；④ 权重灵敏度（40/20/20/20 是拍的）由 strategy_grid 扫 | R22（v0.119/v0.121） |

## 维护约定

- **完成的项直接删**，不留 `~~删除线~~` —— 已完成的事实归入对应研究单元或版本日志。
- 发现旧文档里的行动项已被推翻时，**移到「已失效」表并写清原因**，不要静默删除：
  别人可能正照着做。
- 新增项必须写**出处链接**，否则无法判断它还成不成立。

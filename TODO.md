# 待办清单

> **范围**：跨目录的待跑/待重跑/待收敛项。与同目录 `CHANGELOG.md` 的分工——
> **本文件记「还没做的事」，版本日志记「已经改了的策略规则」**，互不混放。
>
> 优先级按**「它阻塞了什么」**排，不按工作量：
> P0 = 阻塞其他事或 live 正在依赖 ｜ P1 = 已有结论悬空 ｜ P2 = 新验证 ｜ P3 = 技术债
>
> 最后更新：2026-09-14（#63 解析器下沉落地（v0.225）+ #76③ ctx 输入域规范
> 落地（v0.226-0.227：detect(ctx=) 规范 + 12 模块逐个收口，设计稿 §9 回填）；
> #76 只剩 ①影子对照待天数收集；#65 判据预注册落档（v0.228，R33，待生产机
> 跑数）；#64 拍板落地（v0.229：RD 门内提醒 + rsi_state→candidate，整行已删）；
> #61 方向拍板试跑（v0.230：R34 预注册 + score_evolution_study 工具落地，
> 剩余生产机跑数）。此前 2026-09-13：#67 迁移完成 + #76② 闭环）。
> 当前活跃：#60 影子观察（待 owner 拍板）、#61 校准回测、#26 剩余子项挂起、
> #59 剩余机会、#76 遗留验证）

## P0 · 阻塞项

（当前无 P0。）

## P1 · 重跑（结论悬空）

逐条对应研究单元的 `**重跑口径**` 字段，详见
[`research/README.md`「⚠️ 重跑清单」](governance/research/README.md)。

| # | 单元 | 重跑什么 | 前置 |
|---|---|---|---|

（当前无。）

## P2 · 待跑（新验证）

| # | 事项 | 出处 | 备注 |
|---|---|---|---|
| 65 | **「跌破 QSX/DKX 未收复=排除」独立成风控腿研究**（R23 v2 收口后的下一题）：v2 证明排除态是全部边际（hit-only 零价值）——作为可移植风控/回避条件叠加到现有基底与出场上单独测边际；**判据预注册已落档（2026-09-14，v0.228，R33）** | R23 v2（v0.124）| 待生产机跑数（手册见 R33 附录四步；前置工程 qsx_exclusion_free gate 口径已写死，待 owner 拍板实施） |

⚠️ 教训（cross-window 复核，已归入 R10）：edge 集中在单一 regime 的方案首轮看起来都很好。

## P3 · 待收敛 / 技术债

| # | 事项 | 出处 |
|---|---|---|
| 59 | **scorer 双形态剩余机会**：s_shape 系 **✅ 已落地（2026-09-15，v0.236——s_shape/s_reversal/invert_s_shape 三键接 `_SCORER_PRECOMPUTE`，端到端 evaluate_trades 10.2×）**；alpha101/baseline 本就 O(1) 无需旁路；pvcorr/low_vol/momentum/reversal_quality± 纯 df 时序可接（中等优先，留待下轮——每键需独立逐位等价钉测套）；mcap 需股本外部输入不接；b1_dual/long_structure/b2/main_rally 黑盒 detector 不适合。优先级低，随回测批次顺带做 | v0.73 审查（①② 已落地 v0.74；③④ 已否决，见已失效表）；v0.236 s_shape 系 |
| 76 | **#67 收敛的遗留验证**（代码侧全部落地）：②生产机回测清单 **✅ 已完成（2026-09-13，v0.224）**——§4.4 口径 7 格 × 双窗 × 修复前/修复后两版全部逐位一致（产物 `artifacts/logs/factor67_verify/{pre,post}/`）；③ctx 输入域规范专项 **✅ 已实施（2026-09-14，v0.226-0.227）**——`detect(df=None, code="", *, ctx=None)` 规范 + 12 模块逐个落地（设计稿 §9 实施清单）。**只剩 ①影子对照**：1800 候选表标注列 ≥10 交易日与旧口径逐位一致（需天数收集；理论上逐位不变，影子是最后保险） | 2026-09-13 #67 迁移完成；2026-09-14 #76③ 落地（设计稿 `governance/strategy/_factors/interface_unification_plan.md` 已回填） |

## ⚠️ 已失效的行动项（**别照着做**）

旧文档里这些「待跑」已被后续发现推翻或已完成。留着是为了**防止有人照单执行**：

| 原行动项 | 出处 | 为什么失效 |
|---|---|---|
| 「活跃板块（多次上榜）∩ J<13 命中接 1800」 | #26 / v0.77-v0.79 | **2026-08-19 三轮研究证伪**：120 窗 regime 变体曾「过线」（H20 +2.52pp 两半同向），240 错位窗三 horizon 全部前后半翻转（H20：前半 −0.57 / 后半 +2.52）——弱市抗跌、强反弹市滞涨，edge 随 regime 反转抵消（R10/R4 同款单 regime 假象）。`sector_daily_rank` 采集器保留作 #26 回调四项原料，**此假设不接 1800** |
| 「`fetch_pit_financials` 逐期全量台账读写改批量写（~97% IO）」 | #59③ | **owner 2026-08-19 拍板不动（选 C）**：批量补拉是低频运维操作，IO 浪费可接受；逐期落盘的容错语义（一期失败不丢已完成期）与 stdout 逐期数值口径保留 |
| 「东财分页并发拉取」 | #59④ | 改请求时序与限流特征（东财连打会被静默限流，正是 `FetchIncomplete` 防的场景），无法逐位等价钉住，已否决 |
| 「去幸存者偏差：`--data-source qlib`」 | R10 待跑 #3 | **2021 年后一只退市股都没有**，且 `2021_2026` bundle 已弃用（加法调整）⇒ 照做只会引入放大 13~21% 的收益，去偏一点没做到。近期窗口**目前无法去偏**。⚠️ qlib 数据接口已于 2026-08-24（v0.109）整体删除，本条连执行路径都没有了 |
| 「补探止损下界 `pct_03`/`pct_04`」 | R10 待跑 #4 | ✅ 已完成 —— **5% 是崖不是坡**（5%→4% 掉 42%）|
| 「`--exclude-zero-ret` 复跑」 | R3 | ❌ 已证是**错误的修正**（删的是合法样本），已标注勿用 |
| 「V2/V3 重构打分进 live」（TODO #66） | R22 / v0.119-v0.121 | **2026-08-26 独立窗（pre2019）复核翻车**：V2/V3 半窗翻转 + 盈亏比破 2.4 放宽线——判别 edge 属近 regime 富集（2019 后），与 R2/R3/R10 同款 regime 假象。live 打分维持 V0 现行，不回 strategy_grid |

## P4 · strategy 梳理查出的问题（2026-08-06）

索引与分类见 [`strategy/README.md`](governance/strategy/README.md)。

| # | 事项 | 性质 |
|---|---|---|
| 26 | **板块信息利用整体优化**：方向已转清理瘦身（v0.80 四项拍板已执行：1800 主线指纹节/cz 板块否决/资金意图板块腿已删）；剩余子项（回调四项切换检查脚本化、主线题材判定重设计）在清理方向下优先级存疑，**下次 owner 提到时再定是否还做** | 剩余子项挂起待 owner 重提（进展见 CHANGELOG v0.77-v0.80） |

## P5 · contracts 梳理查出的问题（2026-08-06）

索引与核查结论见 [`contracts/README.md`](governance/contracts/README.md)。

（当前无。）

## P6 · trades 梳理（2026-08-06）

（当前无——#32 观察期完成，v0.103 已转硬闸。）

## P7 · 因子层（2026-08-06）

| # | 事项 | 性质 |
|---|---|---|
| 61 | **capital_intent 及因子分值校准回测**（status candidate → active 的前提；#62 迁入的 7 个技术分轴因子的 status 升级同批议）：调 `scoring.weights` 一律先回测（同 cap_rules 纪律）；用 `strategy_grid` 因子轴纳入组合寻优。⚠️ R19（2026-08-21）后重新定位：技术分总量对涨幅无预测力 ⇒ 校准的目标函数**不能**是「预测涨幅」，应是「分层行动语义下的盈亏比/胜率组合」；实跑排在 R11 之后。⚠️ R24（2026-08-28）推进状态：打分重建路线 Phase 3 终审**证伪**（P1/P2/P3 pre2019 半窗翻转，edge 属近 regime）——按预注册退路转「分位数分层」止血方案（治标）；校准目标函数的重新定义仍未解。⚠️ 2026-09-09 工具侧已备：`research/evolution/ic_eval.py` 提供 RankIC/ICIR，`backtest_factors --scorer-expr` 与 strategy_grid `expr:` 轴可让任意 DSL 因子直接进组合寻优——卡点只剩目标函数定义。⚠️ **2026-09-14 方向拍板试跑（v0.230）**：打分系统进化 v1 = 「新轴基因组（腿集合×权重格）」——增量在新轴不在调权（四轮调权证伪的教训）；复合打分编译成单条 DSL 表达式（TS_RANK 归一加权和）复用 strategy_grid 全部机制；R34 预注册落档（判据 C1~C5 写死）+ 工具 `score_evolution_study`/`score_genome` 已落地钉测 | ⚠️ **2026-09-15 R34 跑数判毕：打分进化 v1 证伪收口**——双来源（r34_v1 冒烟池 / r34_v2 score_legs_r1 池）同判结局②：C4 打不过随机臂（top −0.0007 / −0.0039 vs +0.5474）标筛选假象嫌疑；R22-R30 调权四轮之后「增量在新轴」路线同死，打分系五轮重建收口转元层反思（详见 R34 回填区）。打分进化试跑分支到此为止，本条目回到「校准目标函数定义未解」原状 |

## 需要 owner 拍板

| # | 事项 | 出处 |
|---|---|---|
| 60 | **plan 信号并入 SIGNAL_ORDER 与否**：v0.83 影子期观察——14:45/盘后两份报告每日输出「计划判定 vs 现行判定」对比（⚠️影子不一致标注）。**连续 5 个交易日**影子对比后 owner 拍板是否把 plan 信号（plan_stop_breach/plan_tp_scale_out）并入 `b1_holding_state.SIGNAL_ORDER` 转正式判定；切换本身是**独立提交**，不动其他字段。注意：影子期存量持仓多数无计划（早于机制落地），对比样本只含新建仓票。**判读注记（v0.86 review 发现的两个结构性事实）**：① 两份报告的「现行判定」列口径不同——review_core §2.1 比 `classify()` 输出（含 B1 短路/硬风险层），final_close_review §5 比 `b1 final_priority`，同一持仓可能一边「一致」一边「⚠️」，对照时以各自口径为准；② `source=default` 的计划止损=entry×0.93，恰与 live −7% P1 减仓线重合而影子判 P0 ⇒ 每张 default 计划到 −7% **必然**亮「⚠️影子不一致」——这是设计使然的噪音不是信号，统计一致性时应剔除 | 「因子×止盈×止损」架构计划 Phase C（v0.83） |

## 维护约定

- **完成的项直接删**，不留 `~~删除线~~` —— 已完成的事实归入对应研究单元或版本日志。
- 发现旧文档里的行动项已被推翻时，**移到「已失效」表并写清原因**，不要静默删除：
  别人可能正照着做。
- 新增项必须写**出处链接**，否则无法判断它还成不成立。

# CHANGELOG（策略版本记录）

记录策略 Team 的重要规则变化。

| 日期 | 版本 | 修改内容 | 修改原因 | 后续验证指标 |
|---|---|---|---|---|
| 2026-07-09 | v0.1 | 建立策略 Team 初始框架：market_timing、industry_research、theme_tracker、portfolio_review、risk_control、chief_decision、strategy_evolution | 搭建持续进化投资策略 Team | 每日计划是否稳定输出；是否减少计划外交易；风控是否执行 |
| 2026-07-09 | v0.2 | 加入 0AMV 活跃市值做多/空头区间规则 | 用户确认 0AMV 是 A股资金风险偏好关键指标 | 0AMV 空头时是否减少亏损交易 |
| 2026-07-09 | v0.3 | 加入个股服从板块、板块服从大盘规则 | 强化层级决策 | 个股交易是否减少逆板块操作 |
| 2026-07-09 | v0.4 | 加入通用技术走势框架：上涨/下跌/横盘、箱体、KDJ、MACD | 统一板块与个股技术判断 | 箱体和指标信号是否提高买卖质量 |
| 2026-07-09 | v0.5 | 明确 industry_research 负责产业/宏观，theme_tracker 负责板块技术走势 | 调整角色边界 | 产业方向和交易窗口是否区分清楚 |
| 2026-07-13 | v0.6 | 固化盘后六步复盘：新闻、大盘、热点、持仓、选股、次日计划；增加单票仓位不超过20%的硬约束 | 将复盘转化为次日可执行计划并控制集中度 | 六步完整率；次日计划执行率；单票超限次数；超限恢复时效 |
| 2026-07-13 | v0.7 | 核心均线统一为MA25、MA60、MA144、MA240；MA25/60用于短中期趋势判别，MA144/240用于中长期结构和牛熊过滤；20/60日箱体周期保持不变 | 对齐用户实际关注的均线体系，消除MA20旧口径 | 四均线字段完整率；不同均线结构下的后续收益；破位/收复信号有效性；与三状态及B1模型的协同效果 |
| 2026-07-14 | v0.8 | 加入活跃实质空头区间反弹减仓硬规则 | 用户确认该区间内所有反弹均为减仓机会 | 反弹减仓执行率、减仓后回撤规避效果 |
| 2026-07-14 | v0.9 | 加入大坑、小坑、小洞补坑策略及长期形态过滤 | 区分长期修复、局部B1和事件急跌反弹的收益目标与退出纪律 | 各坑型胜率、持有周期、MFE/MAE、结构止损执行率、主线过滤效果 |
| 2026-07-14 | v0.10 | B1增加建仓波、拉升波、冲刺波三类前置拉升识别，并引入第二/第三B1与SB1等待规则 | 同样的J低位和B1形态在不同拉升阶段可靠性差异显著，需先识别筹码阶段和回调路径 | 三类拉升波首个B1胜率；第2/3个B1与SB1胜率；冲刺波首B1规避回撤；MFE/MAE |
| 2026-07-14 | v0.11 | B1增加非一波流三阶段确认：连续温和放量上涨、顶部无放量大阴/显著阴量、B1回调连续明显缩量 | 用上涨健康度、顶部派发风险和回调抛压共同判断前置上涨是否具有延续基础 | 三项联合命中率；首个B1胜率与MFE/MAE；顶部派发撤销后的回撤规避；各阶段量比阈值 |
| 2026-07-14 | v0.12 | B1增加通达信开盘后量比执行分档：<10震荡回调分批买、约20上攻承接分批买、>10且低开约-2%先观察修复再择机建仓 | 将已获许可B1按开盘量价状态区分执行节奏，避免追涨或在分歧下跌中直接接刀 | 各量比分档胜率；不同取值时间稳定性；开盘后MFE/MAE；低开修复成功率；取消信号有效性 |
| 2026-07-14 | v0.13 | 新增每日持股检查手册：忽略普通盘中冲高回落，检查尾盘/次日修复，缩量小阴再等一天，不破BBI且收盘价抬高则继续持有 | 防止冲刺阶段快速洗盘、大盘同步下杀和盘中噪声导致误卖强势持仓 | 误卖减少率；尾盘/次日修复成功率；缩量小阴后续收益；BBI持有规则MFE/MAE；硬风险触发后的执行率 |
| 2026-07-14 | v0.14 | 新增大盘回调后的资金切换与标的四级筛选，并强制检查主题、主题内分化、大小票和高低位切换 | 将大盘调整视为资金重新分配过程，用相对强弱和修复速度执行弃弱留强 | 四类标的后续收益与MFE/MAE；主题切换识别准确率；大小/高低切换持续性；弱反弹清仓回撤规避 |
| 2026-07-14 | v0.15 | B1开盘量比升级为六场景矩阵，增加低量无大跌、中量量价齐升、高量下跌突破日内前高及大幅低开分批试仓规则 | 用量比区间与开盘后价格方向共同判断日内节奏，并解决7-16、<10、>10区间重叠 | 六场景胜率与MFE/MAE；下午量价同步拉升概率；日内前高突破有效性；加速下杀/尾盘建仓差异；参数时点稳定性 |
| 2026-07-15 | v0.16 | B1新增分歧转一致反转K：J<13、极致缩量、收盘涨跌幅-2%至+2%、振幅<=7%四项联合识别，并要求后续修复确认 | 识别B1回调末端抛压衰竭、分歧收敛和潜在反转节点，同时防止单一低J或缩量被误当买点 | 四项联合命中率；次日/三日确认率；不同缩量阈值与分位胜率；MFE/MAE；跌破反转K低点后的失效回撤 |
| 2026-07-15 | v0.17 | B1新增已完成上升N型结构前低清仓点：以L1-H1-更高L2-收盘突破H1确认，L2最低价作为结构硬失效位；接入日报、14:45和正式盘后复盘 | BBI属于动态趋势预警，不能替代结构性硬止损；需要把策略文档已有的N型前低转为可计算、可审计字段 | 三左三右分型窗口敏感性；跌破后回撤规避；假跌破恢复率；与BBI清仓信号的提前/滞后关系；跨行业MFE/MAE |
| 2026-07-15 | v0.18 | 修正N型前低口径：使用收盘价分型；L1为主结构硬清仓位，L2为更高回踩结构位；候选N无需等待突破H1即可记录，突破后再升级确认；保留低点簇盘中极值 | 中银证券实盘核验发现旧算法错误保留突破前已失效的4月候选，并将L2误作唯一硬位；需区分主结构失效与回踩尝试失败 | L1/L2分别失守后的回撤；收盘分型与最低价分型差异；假跌破率；候选N转确认比例；三左三右窗口敏感性 |
| 2026-07-15 | v0.19 | 新增`B1-holding-v1`统一持仓状态：集中裁决硬止损、N型L1/L2、BBI、四均线/箱体、量价、连续中大阳止盈、反转K候选和空头反弹减仓，并供总控、日报、14:45和盘后共用 | 消除B1经验只存在于文档或各报告重复判断的问题，保证同一事实在不同流程节点产生一致优先级 | 跨报告动作一致率；P0/P1误报率；缩量小阴次日修复率；连续中大阳后止盈效果；空头反弹减仓MFE/MAE |
| 2026-07-17 | v0.20 | 架构重构为纯确定性脚本主链：移除 Agent/skill 证据层（02_agents/ 33文件、build_skill_contracts、skill_adapters、stock_pool_builder）；创建 generate_risk_and_sectors.py 替代；治理文档合并为单一 MASTER_WORKFLOW.md；测试独立到 tests/ 目录；消除全部硬编码路径（paths.py + 环境变量）；消除 close_review 包/模块同名冲突（改名 review_core.py）；修复 13 个子目录脚本 BASE 深度 bug（parent.parent→parents[2]）；数据卫生出库（artifacts/reports/daily + artifacts/reports 敏感报告移出 git）；KDJ 阈值对齐（12→13） | 纯脚本模式不再需要多角色 Agent；路径硬编码阻碍迁移和 CI；包/模块同名导致测试断裂；敏感数据不应入库 | 37 tests pass；所有子目录脚本 BASE 解析到项目根；git ls-files 无敏感报告；选股重建时以 DATA_FLOW_CONTRACT 等标注文档为蓝图 |
| 2026-07-19 | v0.21 | 0AMV 空头区间「所有反弹均为减仓机会」由文档规则改为**确定性脚本执行**，并置为最高优先级 | v0.8 定的规则此前只存在于文档，实际是否执行取决于当次 LLM 有没有想起来 | 空头段反弹减仓执行率；减仓后回撤规避幅度 |
| 2026-07-22 | v0.22 | 0AMV regime 状态转移**显式 gate 在 `quality=='confirmed'`**：非确认观测不得触发多空切换 | 未确认的 0AMV 数值切换 regime 会让全链方向判断建立在猜测上 | regime 切换与真值台账一致率；confirmed 缺失时是否正确维持原状态 |
| 2026-07-22 | v0.23 | 盘后复盘由 20:30 提前至 **17:00**；每日选股拆为 **18:00 独立链**，消费 17:00 产出的当日 sector_state / risk_decision | 选股与复盘耦合在同一时点，任一失败会拖垮另一个 | 两链独立成功率；18:00 候选表产出稳定性 |
| 2026-07-23 | v0.24 | 选股分层规则改写：**板块降为提示、不再封顶**，分层由个股（技术结构 × 资金意图）定夺；**J<13 改为硬门槛**；`technical_score` 改有界加权 S_shape(0-100) | 板块封顶会把板块内的好票一票否决；J<13 是 B1 核心却曾不作硬门槛；无界打分无法跨日比较 | 分层分布稳定性；J<13 硬门槛后候选数变化；被板块否决过的票的后续表现 |
| 2026-07-28 | v0.25 | 板块相位（880xxx MACD）接入 live 选股，**仅作 hint / 证据层，不封顶** | 板块相位是继 0AMV 之后第二个 OOS 站得住的增强，但加值幅度（~4-6pp）不足以让它单独否决个股。⚠️ **该依据已于 2026-08-09 R4 重跑中未被复现**（四窗独立效应两正两负 −3.0/−10.9/+13.7pp/牛窗净负；最优格的逐笔期望 −0.60% 反而差于纯 amv 格 +0.41%，组合层好处来自并发上限下的择优而非逐笔 edge）⇒ 板块腿降至 L2「方向不稳」，不再作独立 edge 引用。**本条规则本身无需回退** —— 它当初就只把板块定为 hint/证据层、不封顶不否决，live 至今也只用于候选池密度榜的情境标注 | 板块 hint 与最终分层的一致率；与 0AMV 叠加后的减亏幅度 |
| 2026-07-28 | v0.26 | `01_swing_rules.md` 注入回测校准：§五 止损改为**带空间**（不贴买入 K 最低）；顶注写入「价值分层 / 选股无 alpha」认知 | 回测反复证伪「日线价量形态有短周期 alpha」，live 文档不能继续暗示相反的事；贴最低价的止损在真实盘面执行不了 | 止损带空间后的假止损率；分层是否仍被当作 alpha 使用 |
| 2026-07-30 | v0.27 | **运行门控具备阻断力**：退出码 3（非交易日）/ 4（质量 blocked）/ 5（持仓 blocked），可穿透 daily_pipeline 供 cron 判定；**T-1 数据不得冒充当日**（按 `as_of` 判 stale，按 session 定期望数据日）。⚠️ 同日回退：17:00 硬闸改回 opt-in（`--strict-quality-gate`） | 门控此前只写 JSON 不阻断，等于没有；但硬闸与收紧 stale 判定叠加导致 17:00 盘后复盘直接失败，须先校准再开 | blocked 是否只在真正大面积缺数时出现；17:00 链不再因门控失败 |
| 2026-07-31 | v0.28 | 门控质量评分改**按关键性加权**（0AMV 35 / 宽度 20 / 成交额 20 / 情绪 15 / 海外 10），`blocked` 改为显式覆盖率规则；**加仓授权收紧**：regime 必须属白名单 `{做多, 中性}` 且 0AMV 新鲜 | 原无权重算术平均下「0AMV 全缺 + 其余齐全」= 0.8 恰好判 pass 并授予加仓权；原写法 `regime != "空头"` 在 regime 为空串时为真 ⇒ 方向未知却给加仓权 | regime 未知时不得授权；新增 blocked 场景数应为 0 |
| 2026-08-03 | v0.29 | 18:00 选股链的运行门控**只落盘、不阻断**：不得改 bucket / next_step / 分层、不筛候选，仅在备选表给独立「🚦 数据可信度提示」区块 | 门控若改写分层，live 候选就无法与回测对照，「策略本身选出了什么」将不可回溯 | 选股结果与回测同口径可复现；门控 degraded 时分层无变化 |
| 2026-08-04 | v0.30 | **信号标注层上线**（A 类改动）：候选表标注信号来源与类型，供复盘对账 | 此前无法回答「当天这只票是因为什么被选出来的」 | 标注与实际动作一致率；复盘可追溯率 |
| 2026-08-04 | v0.31 | **全链统一前复权**（owner 拍板）：基于通达信 xdxr 权息本地计算，`adjust="qfq"` 成为默认口径。**止损口径按 B1_w.pdf 纠正**：破位以**收盘价**判定（非盘中）、止损位向下留价位余量、成本区内加时间止损 | 未复权数据把除权跳空当成真实暴跌 ⇒ 假止损、假 J<13、假跌停；盘中触发止损与材料口径不符且实盘更易被噪声打掉 | 除权日不再出现假跳空；收盘止损后的胜率与假止损率变化 |
| 2026-08-06 | v0.32 | **反转K 涨跌幅区间改回对称 ±2%**（此前 −2%~+1.8% 不对称）；`REVERSAL_CHANGE_PCT=2.0`，MIN/MAX 由它派生 | 研究侧 `reversal_quality` 打分器一直用对称 ±2%，两边口径不一致 ⇒ 它与 live 的反转K不是同一个东西，而 R2 结论建立在它上面；统一后两边可比。🔁 **本改反转 2026-08-04 按 B1_w.pdf 的纠偏**（材料两处写「−2% 到 1.8%」），与材料不一致是**有意选择**，已三处留痕 | 反转K 命中数变化；`reversal_quality` 重跑后与 live 是否同口径；R2 结论是否随之变化 |
| 2026-08-07 | v0.33 | **北交所日涨跌幅上限口径修正 20% → 30%**；科创板增补 `689`（CDR）前缀。影响 `technical_monitor` 的 `price_limit / 2` = 中大阳/中大阴门槛：BJ 股由 10% 改为 **15%**，于是 10~15% 的涨/跌不再被判中大阳/中大阴 ⇒ `b1_holding_state` 的 `two_bull_profit_take`（P2 分批止盈）与 `heavy_large_bear`（P1 减仓）在 BJ 股上不再于该区间误触发。仅影响北交所与 689 品种 | 项目里 4 份涨跌幅实现对同一输入给出 3 种答案：研究链两份（`backtest_factors`/`reconcile_qfq`）写正确的 30，**live 两份**（`technical_monitor._infer_price_limit`、`s_shape` fallback）写 20 且只认 `920`、漏 `83/87/43`。北交所竞价交易的涨跌幅比例本就是 30%，这是事实性错误而非口径选择。`technical_monitor` 的数据自纠只能把 10 升到 20、**永远到不了 30**，所以偏差不会被历史波动纠正。⚠️ 两处既有测试原本断言 `920xxx == 20`、**把该 bug 锁死** | BJ 标的的中大阳/中大阴判定数变化；`two_bull_profit_take` 与 `heavy_large_bear` 在 BJ 股上的触发频次变化；未能确认当前是否持有 BJ 标的（`data` 为 gitignore 运行时数据，开发机无快照） |
| 2026-08-07 | v0.34 | **持仓处理优先级不再因「多一条高风险」而降级**：`chief_decision` 里高优先风控命中时原本无条件 `priority='P1'`，会把 B1 已判的 **P0 降成 P1**；改为 `P0` 保持、其余升到 `P1`。⚠️ 这改变 `holding_actions` 的**排序**（该表就是「先动手处理哪个」的顺序） | 实测复现：甲(b1=P0 + 高风险止损) 排在 乙(b1=P0、无风险) **后面** —— 即增加一条高优先风控依据反而降低了紧急度，方向明显反了。同时 14:45 侧（`review_core.classify`）的相关顺序问题**未动**，那是依据新鲜度之争，见待办 #50 | 同时命中 B1 P0 与高风险的持仓是否稳定排在表首；对照修前产出确认只有排序与该行 priority 变化、动作文案不变 |
| 2026-08-07 | v0.35 | **盘中风控依据的优先级按「证据新鲜度」定案**（owner 拍板）：14:45 尾盘动作以**实时行情重算的 B1** 为准，压过同日期标签的 RiskDecision。配套：`risk_decision` 新增 `evidence_date`（取自持仓技术面 `latest_date`）；14:45 报告不再把盘前产出的风控标成「当日」，改标「文件为当日，但证据日是 X」 | 此前 `review_core.classify` 里 B1 短路 return 排在 `high_risk` 之前，行为上已是 B1 优先，但**没有依据、也没在任何文档里定过**，而 README §决策优先级只写「risk_control 拥有否决权」、未区分依据新鲜度。核实后发现关键事实：`generate_risk_and_sectors` 在 `daily_pipeline` 里**不受 session_type 限制** ⇒ **09:05 盘前就产出当日 risk_decision**，其依据是**前一交易日收盘**（当日 K 线尚不存在）。所以争议不是「今天 vs 昨天的文件」，而是「同日期标签、依据 T-1 的风控 vs 用 14:45 实时价重算的 B1」——用实时价是对的。而此前 14:45 报告按**文件名**判「当日」，把 T-1 依据显示成今天的，属「把较旧显示成当日」那一类失真 | 14:45 报告是否正确区分文件日与证据日；同时命中 B1 与高优先风控时，风控理由是否仍在该行可见（v0.34 已加「未消化的高优先风控依据」） |
| 2026-08-07 | v0.36 | **反转K 判定阈值收敛到唯一来源 `src/core/b1_thresholds.py`（L0）**，并把收盘涨跌幅判定改为**按显示精度（2 位小数）比较**。两项后果：① `B1_REVK_*` 环境变量此前只对**选股链**生效，14:45/17:00 走的**持仓链**（`technical_monitor` 硬编码 `-2 <= change_pct <= 2`、`amplitude <= 7`；`b1_holding_state` 硬编码 `j < 13`）无视配置 —— 现在两链一起变；② 恰好 ±2.00% 的收盘不再被浮点噪声随价位任意判出。⚠️ 研究侧 `factors/reversal_quality` **刻意不跟随**环境变量（钉死才能复现既有回测数字，R2 P1 依赖），默认值与 live 相同 | owner 2026-08-06 要求「对称 ±2% **且可配置**」，但实测「可配置」只覆盖一半的链，且 `technical_monitor` 的 `thresholds` 字典还把写死的 `[-2.0, 2.0]` 当「当前阈值」上报 ⇒ 配置一改它就在谎报。浮点方面：`(last/prev-1)*100` 在 10.00→9.80 得 -1.9999999999999907（判进）、在 50.00→49.00 得 -2.0000000000000018（判出）；枚举 1.00~100.00 的两位价格，**显示为恰好 ±2.00% 的 19802 组价格对里有 5143 组被判出区间**，而候选 JSON 里写的是 `-2.0` —— 读报告的人看到「-2.0%，在 ±2% 内」却找不到反转K 标记，无从解释。取整后判定与显示一致 | 反转K 命中数变化（预期仅在恰好 ±2.00% 处增加）；设 `B1_REVK_CHG_PCT` 后选股链与持仓链的反转K 结论是否一致；`thresholds` 上报值是否等于生效值 |
| 2026-08-10 | v0.37 | **`overseas_market.as_of` 不再伪造**（owner 拍板走「生产者不编」这条路）：一个 symbol 都没给时间戳时写 `as_of: None` + `as_of_basis: "no_timestamp_from_any_symbol"`，采集时刻改存独立键 `collected_at`。运行门控随之把 overseas 从 `confirmed` 降到 `candidate`（不是 `missing` —— 值还在，只是新鲜度不可证）。⚠️ **整体 `status` 不变**（完整数据下实测仍 `pass`）：overseas 权重 10/100 且被排除在 `core` 覆盖率判定之外，这层隔离已由测试钉住 —— 若哪天 `core` 的排除规则改了，本修复会静默变成一个阻断源 | 门控判据是「有值 **且** `as_of` 非空」，而生产者在无时间戳时填的是**采集时刻** ⇒ 一批没有任何时间戳的数字拿到最强新鲜度结论。**这不是罕见分支**：Yahoo 不可达时走的 TDX ext 降级 `tdx_ext_quotes.fetch_ext_change` 根本不返回 `last_timestamp` ⇒ Yahoo 全挂时必然走到这里，而那恰恰是最需要判新鲜度的时候（实测那一跑的 `sox_change_pct` 还是 SOXX ETF 的**代理值**，`proxy_note` 有记，门控照样报「新鲜已确认」）。⚠️ 门控还忽略了两个**已有**的留痕字段：`as_of_basis` 与该 section 自己的 `quality`（实测 `degraded`）—— 信号是齐的，只是没人读。与契约层已拍板的 `amv_0.as_of` 允许 None 同一原则，原话「编一个 as_of 等于给门控一个假的新鲜度」 | overseas 在门控里的 quality 分布（预期 Yahoo 降级日从 confirmed 变 candidate）；整体 `status` 与 `market_quality` 评分是否如预期不变；`collected_at` 是否只用于排障、不被任何新鲜度判定读取 |
| 2026-08-10 | v0.38 | **当日振幅口径统一为 `(最高价−最低价)/前收盘价`**（owner 拍板），并收敛到**全项目唯一实现** `indicators.amplitude_pct`。⚠️ **live 判定变化只在持仓链**：`technical_monitor` 原用 `(high/low − 1)`（分母是**当日最低价**），现改为规范口径 ⇒ 14:45/17:00 报告的 `amplitude_pct` 与反转K 观察项判定随之变化；选股链（`enrich_candidates`）与 `s_shape`/`backtest_factors`/`reversal_quality` 本就是规范口径，收敛后数值**逐位不变**（10 万组合成数据验证 0 分歧）。刻意保留一处不同：`s_shape` 的 VCP 压缩度分母用**当日收盘**，因为它只作 `recent/prior` 比值参与打分、分母口径在比值里基本抵消，换掉会改 live 的 S 分 —— 该豁免由「源码里必须写下『刻意不同』」的守卫强制 | 清点发现**五份**内联实现（选股链、持仓链、`s_shape`、`backtest_factors`、`reversal_quality`），各自来自独立特性、当时无共享出口可用。其中 `technical_monitor` 的分母是当日最低价，与治理文档明文（`01_swing_rules.md`「当日振幅优先按 `(最高价 - 最低价) / 前收盘价` 计算」）及另四份都不一致。⚠️ 差异不是小数点问题：合成 20 万根日 K 实测，**约 2% 在 7% 门槛上给出相反结论**，且方向是 `low < 前收`（正是反转K 要找的缩量回踩形态）时旧式**更严** ⇒ **同一支票在选股链与持仓链可能得出相反的反转K 结论**。实例：前收 10.00 / 低 9.00 / 高 9.65 —— 规范口径 6.50%（判入）、旧式 7.22%（判出）。⚠️ 另订正 `governance/strategy/README.md` 声称「`amplitude_pct` 计算式一致」——对持仓链不成立 | 持仓链 `amplitude_pct` 数值变化（预期整体略降，`low<前收` 时降幅最大）；反转K 观察项在 14:45/17:00 的命中数变化；同一支票两链的反转K 结论是否恒一致 |
| 2026-08-09 | v0.39 | **反转K 判定逻辑在 live 与研究两侧对齐**（补记，2026-08-10 清点时发现当时漏记）。live 侧：`factors/s_shape` 的反转K 改用 `b1_thresholds.change_in_range`（**round-2 显示精度判定**，v0.36 拍板的口径），振幅分母 `low` → `前收`。s_shape 在 live 路径上（`score_candidates.technical_score` → bucket → 盘前日报的 A/B 过滤）⇒ 属 live 判定变化。研究侧：`backtest_factors.reversal_k_gate` 同步改 round-2、量分位方向 `<=` → `<`（与 live 同向）。⚠️ **常量仍钉死 live 默认值、不读 `B1_REVK_*` env** —— 钉死的是阈值数，对齐的是判定逻辑，两件事分开 | 反转K 在 live 与研究两侧「不是同一个东西」这件事，v0.32 只统一了**阈值**（对称 ±2%），判定逻辑仍有三处分歧：涨跌幅取整精度、振幅分母、量分位的 `<=` vs `<`。不对齐则「拿回测结论解释 live」这件事在反转K 上不成立。⚠️ **可复现性未受损**：清点确认**没有任何已记录的研究结果用过 `--entry-filter reversal_k`**（R4 重跑用的是 `j_low`，R9/R10/R11 用的是止损/分批臂）⇒ 逻辑变更没有孤儿数字。⚠️ 同批的 QSX×3 / MACD×4 / ADX×2 去重**不在本条范围**：提交侧已做数值等价验证（9 个出口对合成序列逐位相同）⇒ 纯去重、零行为变化，不构成规则变化 | 反转K 在两侧的命中集合是否可比（同数据同参数应一致，模 env 覆盖）；`s_shape` 的 S 分变化幅度（预期仅在 round-2 边界与 `low<前收` 时出现） |
| 2026-08-10 | v0.40 | **择时评分的陈旧判定改听「生产者声明的 quality」**（owner 拍板走窄修法）：`market_timing_scorer.is_stale` 的判据由 `quality == "stale"` 改为 `quality in SECTION_NOT_FRESH`（`{stale, raw_only, degraded, missing}`）。影响：宽度/情绪/成交额三段在 `raw_only`（无数据日）或 `degraded`（数据日与预期不符）时**不再按当日满分计入** —— 实测 `score_breadth` 由 **11/15 降到中性 7.5**，`score_sentiment` → 7.5、`score_turnover` 8 → 4，且归因文案会写明原因。配套在 `contracts.py` 立 `SECTION_QUALITY` / `SECTION_NOT_FRESH` 两个词表（L0，两个生产者与消费者共用）| 两个生产者用的是**不同词表**而消费者只认一个词：`merge_incremental_market.section_quality` 出 auto/stale/**raw_only**，`market_timing_collector._freshness` 出 auto/**degraded**（+missing 分支），而 `is_stale` 只认 `"stale"` ⇒ **`raw_only` 与 `degraded` 被当成新鲜**。实测那 11 分的归因文案是「涨跌比 1.24。」—— 读报告的人无从知道分数建立在「不知道是哪天的数据」上。⚠️ 触发路径是真的：`collect_incremental_market` 在索引非日期时写 `date: ''`（而「mootdx Reader 返回 DatetimeIndex 而非列」是记录在案的反复踩坑点），merge 据此写 `quality: raw_only` —— **生产者已如实声明，只是没人听**。与 #52 同一形状（`as_of_basis` 留了痕而门控不读）。⚠️ **刻意用窄修法**：判据是「生产者声明」而非「有没有 as_of」——后者是更宽的 fail-closed，会把合法但未写 as_of 的段也打成陈旧 | 三段 quality 的分布（预期 raw_only/degraded 日的评分从 11 降到 7.5）；`market_quality` 总分与 status 变化幅度；归因文案是否都写明了 stale 原因 |

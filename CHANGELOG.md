# CHANGELOG（策略版本记录）

记录策略 Team 的重要规则变化。

| 日期 | 版本 | 修改内容 | 修改原因 | 后续验证指标 |
|---|---|---|---|---|
| 2026-07-09 | v0.1 | 建立策略 Team 初始框架：market_timing、industry_research、theme_tracker、portfolio_review、risk_control、chief_decision、strategy_evolution | 搭建持续进化投资策略 Team | 每日计划是否稳定输出；是否减少计划外交易；风控是否执行 |
| 2026-07-09 | v0.2 | 加入 0AMV 活跃市值做多/空头区间规则 | 用户确认 0AMV 是 A股资金风险偏好关键指标 | 0AMV 空头时是否减少亏损交易 |
| 2026-07-09 | v0.3 | 加入个股服从板块、板块服从大盘规则 | 强化层级决策 | 个股交易是否减少逆板块操作 |
| 2026-07-09 | v0.4 | 加入通用技术走势框架：上涨/下跌/横盘、箱体、KDJ、MACD | 统一板块与个股技术判断 | 箱体和指标信号是否提高买卖质量 |
| 2026-07-09 | v0.5 | 明确 industry_research 负责产业/宏观，theme_tracker 负责板块技术走势 | 调整角色边界 | 产业方向和交易窗口是否区分清楚 |
| 2026-07-13 | v0.6 | 固化盘后六步复盘（新闻、大盘、热点、持仓、选股、次日计划）；单票仓位 ≤20% 硬约束 | 复盘转化为次日可执行计划并控制集中度 | 六步完整率；次日计划执行率；单票超限次数与恢复时效 |
| 2026-07-13 | v0.7 | 核心均线统一为 MA25/60（短中期趋势）与 MA144/240（中长期结构与牛熊过滤）；20/60 日箱体不变 | 对齐用户实际关注的均线体系，消除 MA20 旧口径 | 四均线字段完整率；各均线结构的后续收益；破位/收复信号有效性 |
| 2026-07-14 | v0.8 | 加入活跃实质空头区间反弹减仓硬规则 | 用户确认该区间内所有反弹均为减仓机会 | 反弹减仓执行率；减仓后回撤规避效果 |
| 2026-07-14 | v0.9 | 加入大坑、小坑、小洞补坑策略及长期形态过滤 | 区分长期修复、局部 B1 和事件急跌反弹的收益目标与退出纪律 | 各坑型胜率、持有周期、MFE/MAE、结构止损执行率、主线过滤效果 |
| 2026-07-14 | v0.10 | B1 增加建仓波/拉升波/冲刺波三类前置拉升识别，引入第 2/3 个 B1 与 SB1 等待规则 | 同样的 J 低位与 B1 形态在不同拉升阶段可靠性差异显著 | 三类拉升波首个 B1 胜率；第 2/3 个 B1 与 SB1 胜率；冲刺波首 B1 规避回撤；MFE/MAE |
| 2026-07-14 | v0.11 | B1 增加非一波流三阶段确认：连续温和放量上涨、顶部无放量大阴、B1 回调连续明显缩量 | 用上涨健康度、顶部派发风险与回调抛压共同判断延续基础 | 三项联合命中率；首个 B1 胜率与 MFE/MAE；顶部派发撤销后的回撤规避 |
| 2026-07-14 | v0.12 | B1 增加开盘后量比执行分档：<10 震荡回调分批买、约 20 上攻承接分批买、>10 且低开约 −2% 先观察修复再择机 | 按开盘量价状态区分执行节奏，避免追涨或分歧下跌中接刀 | 各量比分档胜率；取值时点稳定性；低开修复成功率 |
| 2026-07-14 | v0.13 | 新增每日持股检查手册：忽略普通盘中冲高回落，查尾盘/次日修复，缩量小阴再等一天，不破 BBI 且收盘价抬高则继续持有 | 防冲刺洗盘、大盘同步下杀与盘中噪声误卖强势持仓 | 误卖减少率；尾盘/次日修复成功率；缩量小阴后续收益；BBI 持有规则 MFE/MAE |
| 2026-07-14 | v0.14 | 新增大盘回调后的资金切换与标的四级筛选（主题、主题内分化、大小票、高低位切换强制检查） | 把大盘调整视为资金重新分配，用相对强弱与修复速度弃弱留强 | 四类标的后续收益与 MFE/MAE；主题切换识别准确率；切换持续性 |
| 2026-07-14 | v0.15 | B1 开盘量比升级为六场景矩阵，增加低量无大跌、中量量价齐升、高量下跌突破日内前高及大幅低开分批试仓 | 用量比区间 × 开盘后价格方向判断日内节奏，解决 7-16/<10/>10 区间重叠 | 六场景胜率与 MFE/MAE；日内前高突破有效性；参数时点稳定性 |
| 2026-07-15 | v0.16 | B1 新增分歧转一致反转K：J<13、极致缩量、收盘涨跌幅 ±2%、振幅 ≤7% 四项联合，并要求后续修复确认 | 识别 B1 回调末端抛压衰竭与分歧收敛；防单一低 J 或缩量被误当买点 | 四项联合命中率；次日/三日确认率；MFE/MAE；跌破反转K 低点后的失效回撤 |
| 2026-07-15 | v0.17 | B1 新增已完成上升 N 型结构前低清仓点（L1-H1-更高 L2-收盘突破 H1 确认，L2 最低价为结构硬失效位）；接入日报、14:45 与盘后复盘 | BBI 是动态预警，不能替代结构性硬止损；把文档已有的 N 型前低转为可计算、可审计字段 | 分型窗口敏感性；跌破后回撤规避；假跌破恢复率；与 BBI 清仓信号的提前/滞后关系 |
| 2026-07-15 | v0.18 | 修正 N 型前低口径：用收盘价分型；L1 为主结构硬清仓位、L2 为更高回踩位；候选 N 无需等突破 H1 即可记录 | 实盘核验发现旧算法错误保留已失效候选，且把 L2 误作唯一硬位 | L1/L2 分别失守后的回撤；收盘分型 vs 最低价分型差异；假跌破率；候选转确认比例 |
| 2026-07-15 | v0.19 | 新增 `B1-holding-v1` 统一持仓状态：集中裁决硬止损、N 型 L1/L2、BBI、四均线/箱体、量价、连续中大阳止盈、反转K 候选与空头反弹减仓，供总控/日报/14:45/盘后共用 | 消除 B1 经验只在文档或各报告重复判断的问题，同一事实在各节点产生一致优先级 | 跨报告动作一致率；P0/P1 误报率；缩量小阴次日修复率；中大阳止盈效果 |
| 2026-07-17 | v0.20 | 架构重构为纯确定性脚本主链：移除 Agent/skill 证据层（33 文件），新建 generate_risk_and_sectors.py 替代；治理文档合并为单一 MASTER_WORKFLOW.md；测试独立到 tests/；路径全部收敛 paths.py + 环境变量；修复 13 个子目录脚本 BASE 深度 bug；敏感报告移出 git；KDJ 阈值对齐（12→13） | 纯脚本模式不再需要多角色 Agent；路径硬编码阻碍迁移与 CI；敏感数据不应入库 | 测试全过；子目录脚本 BASE 解析到项目根；git ls-files 无敏感报告 |
| 2026-07-19 | v0.21 | 0AMV 空头区间「所有反弹均为减仓机会」由文档规则改为**确定性脚本执行**，置最高优先级 | 该规则此前只在文档里，执行与否取决于当次 LLM 想没想起来 | 空头段反弹减仓执行率；减仓后回撤规避幅度 |
| 2026-07-22 | v0.22 | 0AMV regime 切换**显式 gate 在 `quality=='confirmed'`**：非确认观测不得触发多空切换 | 未确认数值切换 regime 会让全链方向判断建立在猜测上 | regime 切换与真值台账一致率；confirmed 缺失时是否正确维持原状态 |
| 2026-07-22 | v0.23 | 盘后复盘由 20:30 提前至 **17:00**；每日选股拆为 **18:00 独立链**（消费 17:00 产出的 sector_state / risk_decision） | 选股与复盘耦合在同一时点，任一失败拖垮另一个 | 两链独立成功率；18:00 候选表产出稳定性 |
| 2026-07-23 | v0.24 | 选股分层改写：**板块降为提示、不再封顶**，分层由个股（技术结构 × 资金意图）定夺；**J<13 改为硬门槛**；`technical_score` 改有界加权 S_shape(0-100) | 板块封顶会一票否决板块内好票；J<13 是 B1 核心却曾不作硬门槛；无界打分无法跨日比较 | 分层分布稳定性；J<13 硬门槛后候选数变化；被板块否决过的票的后续表现 |
| 2026-07-28 | v0.25 | 板块相位（880xxx MACD）接入 live 选股，**仅作 hint/证据层，不封顶**。⚠️ 该依据在 2026-08-09 R4 重跑未复现（四窗两正两负）⇒ 板块腿降至 L2「方向不稳」；本条规则无需回退——当初就只定为 hint、不否决 | 板块相位是继 0AMV 之后第二个 OOS 站得住的增强，但加值幅度（~4-6pp）不足以单独否决个股 | 板块 hint 与最终分层的一致率；与 0AMV 叠加后的减亏幅度 |
| 2026-07-28 | v0.26 | `01_swing_rules.md` 注入回测校准：§五 止损改为**带空间**（不贴买入 K 最低）；顶注写入「价值分层 / 选股无 alpha」认知 | 回测反复证伪日线价量短周期 alpha，live 文档不能暗示相反的事；贴最低价的止损实盘执行不了 | 止损带空间后的假止损率；分层是否仍被当作 alpha 使用 |
| 2026-07-30 | v0.27 | **运行门控具备阻断力**：退出码 3（非交易日）/4（质量 blocked）/5（持仓 blocked），可穿透 daily_pipeline 供 cron 判定；**T-1 数据不得冒充当日**（按 `as_of` 判 stale）。⚠️ 同日回退：17:00 硬闸改回 opt-in | 门控此前只写 JSON 不阻断等于没有；但硬闸与收紧 stale 叠加导致 17:00 链直接失败，须先校准再开 | blocked 是否只在真正大面积缺数时出现；17:00 链不再因门控失败 |
| 2026-07-31 | v0.28 | 门控质量评分改**按关键性加权**（0AMV 35/宽度 20/成交额 20/情绪 15/海外 10），`blocked` 改显式覆盖率规则；**加仓授权收紧**：regime 必须属 `{做多, 中性}` 且 0AMV 新鲜 | 原算术平均下「0AMV 全缺+其余齐全」=0.8 恰好 pass 并授予加仓权；`regime != "空头"` 在空串时为真 ⇒ 方向未知却给加仓权 | regime 未知时不得授权；新增 blocked 场景数应为 0 |
| 2026-08-03 | v0.29 | 18:00 选股链的运行门控**只落盘、不阻断**：不改 bucket/next_step/分层、不筛候选，仅在备选表给独立「🚦 数据可信度提示」区块 | 门控若改写分层，live 候选就无法与回测对照，「策略选出了什么」不可回溯 | 选股结果与回测同口径可复现；门控 degraded 时分层无变化 |
| 2026-08-04 | v0.30 | **信号标注层上线**：候选表标注信号来源与类型，供复盘对账 | 此前无法回答「这只票当天是因为什么被选出来的」 | 标注与实际动作一致率；复盘可追溯率 |
| 2026-08-04 | v0.31 | **全链统一前复权**（owner 拍板）：基于通达信 xdxr 权息本地计算，`adjust="qfq"` 为默认口径。**止损口径按 B1_w.pdf 纠正**：破位以收盘价判定、止损位向下留余量、成本区内加时间止损 | 未复权数据把除权跳空当真实暴跌 ⇒ 假止损、假 J<13、假跌停；盘中触发止损与材料口径不符且更易被噪声打掉 | 除权日不再出现假跳空；收盘止损后的胜率与假止损率变化 |
| 2026-08-06 | v0.32 | **反转K 涨跌幅区间改回对称 ±2%**（此前 −2%~+1.8% 不对称）；`REVERSAL_CHANGE_PCT=2.0`，MIN/MAX 由它派生。🔁 本改反转 2026-08-04 按 B1_w.pdf 的纠偏，与材料不一致是**有意选择**（已三处留痕） | 研究侧 `reversal_quality` 一直用对称 ±2%，两边口径不一致 ⇒ 它与 live 的反转K 不是同一个东西，而 R2 结论建立在它上面 | 反转K 命中数变化；`reversal_quality` 重跑后与 live 是否同口径；R2 结论是否随之变化 |
| 2026-08-07 | v0.33 | **北交所日涨跌幅上限修正 20%→30%**，科创板增补 `689`（CDR）前缀。影响中大阳/大阴门槛：BJ 股由 10% 改为 15% ⇒ `two_bull_profit_take`（P2 止盈）与 `heavy_large_bear`（P1 减仓）在 BJ 股上不再于 10~15% 区间误触发 | 全仓 4 份涨跌幅实现给出 3 种答案：live 两份写 20 且漏 `83/87/43` 前缀，而 BJ 竞价上限本就是 30%（事实错误非口径选择）；既有测试还断言 `920xxx == 20` 把 bug 锁死 | BJ 标的中大阳/大阴判定数变化；两个止盈/减仓规则在 BJ 股的触发频次变化 |
| 2026-08-07 | v0.34 | **持仓处理优先级不再因「多一条高风险」而降级**：高优先风控命中时原会把 B1 已判 P0 降成 P1，改为 P0 保持、其余升 P1。⚠️ 改变了 `holding_actions` 的排序（该表即处理顺序） | 实测：甲（P0+高风险止损）排在乙（P0、无风险）后面——多一条依据反而降低紧急度，方向反了 | 同时命中 B1 P0 与高风险的持仓是否稳定排表首；对照修前产出只有排序与该行 priority 变化 |
| 2026-08-07 | v0.35 | **盘中风控依据按「证据新鲜度」定案**（owner 拍板）：14:45 以**实时行情重算的 B1** 为准，压过同日期标签的 RiskDecision。配套：`risk_decision` 新增 `evidence_date`；14:45 报告改标「文件为当日，但证据日是 X」 | 关键事实：risk_decision 在 09:05 盘前就产出、依据是**前一交易日收盘**；14:45 报告此前按文件名判「当日」，把 T-1 依据显示成今天的——「把较旧显示成当日」类失真 | 14:45 报告是否正确区分文件日与证据日；同时命中 B1 与高风险时风控理由是否仍可见 |
| 2026-08-07 | v0.36 | **反转K 判定阈值收敛到唯一来源 `b1_thresholds.py`（L0）**，收盘涨跌幅判定改为**按显示精度（round-2）比较**。后果：① `B1_REVK_*` env 此前只对选股链生效，持仓链硬编码无视配置——现在两链一起变；② 恰好 ±2.00% 的收盘不再被浮点噪声随价位任意判出（50.00→49.00 在 raw 下是 −2.0000000000000018）。⚠️ 研究侧 `reversal_quality` **刻意不跟随** env（钉死才能复现既有回测数字），默认值与 live 相同 | 「可配置」只覆盖一半的链，且 `technical_monitor` 把写死的 `[-2.0, 2.0]` 当「当前阈值」上报 ⇒ 配置一改它就在谎报；浮点方面实测显示为恰好 ±2.00% 的价格对里有 5143 组被 raw 判出区间 | 反转K 命中数变化（预期仅在恰好 ±2.00% 处增加）；设 env 后两链反转K 结论是否一致；`thresholds` 上报值是否等于生效值 |
| 2026-08-10 | v0.37 | **`overseas_market.as_of` 不再伪造**（owner 拍板）：无时间戳时写 `as_of: None` + `as_of_basis`，采集时刻改存独立键 `collected_at`；门控随之把 overseas 从 `confirmed` 降到 `candidate`。⚠️ 整体 `status` 不变（overseas 权重 10/100 且被排除在 `core` 覆盖率判定之外，已由测试钉住） | 门控判据是「有值且 `as_of` 非空」，而生产者无时间戳时填的是采集时刻 ⇒ 一批无时间戳的数字拿到最强新鲜度结论；Yahoo 全挂走 TDX ext 降级时**必然**走到这条路径，那恰是最需要判新鲜度的时候 | overseas 在门控里的 quality 分布（预期 Yahoo 降级日 confirmed→candidate）；整体 status 与评分是否如预期不变 |
| 2026-08-10 | v0.38 | **当日振幅口径统一为 `(最高价−最低价)/前收盘价`**（owner 拍板），收敛到唯一实现 `indicators.amplitude_pct`。⚠️ live 判定变化只在持仓链：`technical_monitor` 原分母是**当日最低价**（合成 20 万根日 K 实测约 2% 在 7% 门槛上结论相反）⇒ 14:45/17:00 的 `amplitude_pct` 与反转K 观察项随之变化；选股链与研究侧本就规范口径，收敛后逐位不变。刻意保留 `s_shape` VCP 压缩度用当日收盘作分母（只作比值参与打分，换掉会改 S 分；豁免由守卫强制写明理由） | 清点发现五份内联实现，其中 technical_monitor 的分母与治理文档明文及其余四份都不一致 ⇒ 同一支票在选股链与持仓链可能得出相反的反转K 结论 | 持仓链 `amplitude_pct` 数值变化（预期整体略降）；反转K 观察项在 14:45/17:00 的命中数变化；两链结论是否恒一致 |
| 2026-08-09 | v0.39 | **反转K 判定逻辑在 live 与研究两侧对齐**（补记）。live：`s_shape` 反转K 改用 `change_in_range`（round-2，v0.36 口径）、振幅分母 low→前收；研究：`backtest_factors.reversal_k_gate` 同步 round-2、量分位 `<=`→`<`。⚠️ 常量仍钉死 live 默认值不读 env——钉死的是阈值数，对齐的是判定逻辑 | v0.32 只统一了阈值，判定逻辑仍有三处分歧（取整精度、振幅分母、量分位方向），不对齐则「拿回测结论解释 live」在反转K 上不成立。清点确认无已记录结果用过 `--entry-filter reversal_k` ⇒ 无孤儿数字 | 两侧命中集合是否可比（同数据同参数应一致）；`s_shape` 的 S 分变化幅度 |
| 2026-08-10 | v0.40 | **择时评分的陈旧判定改听「生产者声明的 quality」**（owner 拍板窄修法）：`is_stale` 判据由 `== "stale"` 改为 `in SECTION_NOT_FRESH`（`{stale, raw_only, degraded, missing}`）。影响：宽度/情绪/成交额三段在 `raw_only`/`degraded` 时不再按当日满分计入（实测 score_breadth 由 11/15 降到中性 7.5）。配套在 `contracts.py` 立 `SECTION_QUALITY`/`SECTION_NOT_FRESH` 词表 | 两个生产者用不同词表而消费者只认 `"stale"` ⇒ `raw_only` 与 `degraded` 被当成新鲜：生产者已如实声明，只是没人听。刻意用窄修法（听声明而非查 as_of），更宽的 fail-closed 会误伤合法未写 as_of 的段 | 三段 quality 分布（预期 raw_only/degraded 日评分 11→7.5）；总分与 status 变化幅度；归因文案是否写明 stale 原因 |
| 2026-08-11 | v0.41 | **目录重组与真包化**：`07_tools/` → `src/custos/` 可安装包（hatchling 可编辑安装；core=L0 基建+L2 因子/台账、datasource=L1 采集、pipeline=L3 生产+L4 编排、research=L4 研究）；顶层目录去编号（00_governance→governance、01_data→data、03/04/06 三合为 artifacts/{reports,logs}，版本三文档上根）；**sys.path 注入机制全删**（import 唯一事实来源 = 包安装）；静态检查套件入库（`scripts/audit.sh`/`.ps1`，含 ruff format 门槛与 mypy Linux 压噪配置）。判定/报告口径未动 | 平铺 import + 95 处 sys.path 注入让每次搬迁都留死路径地雷（subprocess 引用无 import 图可查）；包化后 import 可静态分析、可安装、CI 零改动 | 全量测试 3857 passed/13 skipped（含分层/路径守卫）；audit 套件可跑；`uv run python -c "import custos.core.paths"` 与各 runner `--help` 冒烟通过 |
| 2026-08-11 | v0.42 | **mypy 渐进类型化五批收口**：core（两批）→ datasource → pipeline → research 全仓注解完成，可操作项 225 → 0（Linux 压噪配置 `scripts/mypy.linux.ini` 下）。规则：公共入口优先注解、推断噪音用签名级注解、未用 `# type: ignore` 漫灌、行为零变化。过程中甄别修复 1 个真 bug（`launch_point_study` 窗口枚举 `since=None` 被吞成空 regime，commit `2b6abf4`） | 静态检查发现的类型债务，core 是依赖枢纽先行；不追求零注解覆盖，只清错误 | 全量测试 3857 passed/13 skipped；mypy 全仓 0 errors；`ruff format --check` 通过 |
| 2026-08-11 | v0.43 | **涨跌幅判定精度全仓统一 round-2（#56 保留项整改，owner 拍板）**：`technical_monitor` 判定链（小阴/大阴/反转K 的 change_pct、bull_metrics 日涨幅）、`enrich_candidates._close_ret_pct` 的 20 日相对强度、海外双生产者（`overseas_market_collector` Yahoo 回退两处、`tdx_ext_quotes` digits 3→2）、A50-CNH 显示（`collect_incremental_market`）全部收口 round-2；研究侧 `backtest_factors` 判定路径（中大阳/大阴向量化 chg、成本区逃脱 escaped）对齐 live，**统计路径（rest_ret/scaled_ret/ret_dd/feats）保持 raw**。⚠️ 判定边界在 ±0.005% 尾差带内移动：raw −2.0000000001 原在 [-2,0) 半开区间外，round-2 后 −2.0 在内——这是整改目的，属口径变更 | v0.36 已定「判定精度 = 显示精度」但只落地了反转K 一处，其余判定/显示仍各用 raw/round-3/不取整 ⇒ 同一涨跌幅在不同链上精度不一、边界归属随价位漂；此次把该原则铺满全仓 | 小阴/大阴/中大阳/成本区逃脱判定数变化（预期仅在尾差带内移动）；live 与回测同数据同参数判定一致率；全量测试与静态检查不回归 |
| 2026-08-12 | v0.44 | **#48 RSS 代码命中加紧邻量词否定**（owner 拍板方案 A）：代码命中除数字边界外，命中数字后 0~2 个空白内紧跟金额/计数量词（`code_unit_suffixes`，走 RSS_FILTER_CONFIG）时不计命中——「净利润600000元」类「金额=代码」误配曾拿 +45 分（单项最大）并凭排序首要键压过 S 级政策登顶；「600000浦发银行」「（600000）分红每10股派2元」真命中不误杀。**#18 跨 bundle 口径混合收口**：`load_bars_csv` 补出声告警（已弃用加法口径 bundle 2021_2026 的副本，百分比收益系统性放大 13~21%，仅应用于诊断对照）；R15 stale 行「s_data(qlib) 收益率口径正确」改为分 bundle 表述（2006_2020 乘法可用、2021_2026 加法已弃用） | #48：数字边界修不掉「代码恰好等于独立金额」的误配；#18：csv 源零检查零告警，但仍有正当诊断调用方（`probe_data_sources`「CSV 冗余副本」探针、四个研究工具的 `--data-source csv` 对照选项）⇒ 只加告警不 deprecate | RSS 候选首位不再是金额误配；csv 源每次加载 stderr 有告警（按 root 去重）；全量测试与静态检查不回归 |
| 2026-08-12 | v0.45 | **宇宙/窗口钉死开关默认改为开（#17，owner 拍板）**：`m2_stop_sweep` 的 `--pin-universe` 默认开（`--dump-codes` 落表共用）、`--window` 默认钉 `DEFAULT_WINDOW`（2024-08-01~2026-08-05，R13/run_m2_sweep.cmd 约定口径，约 490 根 ≈ 旧 `--count 500` 实际窗口）；显式关闭用新增的 `--no-window` / `--no-pin-universe`（跟随 `--no-trades-confirmed` 的正反 flag 惯例）。`--cross-window` 自带 2022-2024 窗口，默认钉死自动让位（显式 `--window` 同给仍报错；`--window` 与 `--no-window` 同给 fail-closed 报错）。⚠️ 历史命令行为变化已确认接受：不带开关的旧命令现在跑出钉死批次（指纹带 `_w*_u`），与未钉旧批不混汇总 | 默认关意味着「忘了给开关就不可复现」——R13 实测扫描期间宇宙 5535→5536、同参数笔数 1106/1092/1087 漂移；可复现应该是默认而不是可选项 | 新批次默认带 `_w20240801-20260805_u` 指纹；`--no-*` 关闭后报表仍打「本批不可复现」警告；全量测试与静态检查不回归 |
| 2026-08-12 | v0.46 | **止损余量新增百分比/ATR 两种风险单位口径（#20，owner 拍板「两个都实现、同批对照再定取舍」）**：`backtest_factors` 新增 `--stop-buffer {tick,pct,atr}` + `--stop-pct-buffer`（默认 0.3 ≈ 10 元股现行 tick_3）+ `--stop-atr-buffer`（默认 0.2×ATR(14)，Wilder `indicators.atr_series` 新增）；**tick 固定金额模式保留但标记设计缺陷**（R10：3 tick 在 5 元股=0.6%、50 元股=0.06%，价格水平混进风险量）。默认 tick ⇒ 不传新参数行为逐位不变；pct/atr 与 tick_buffer>0 互斥（同给 fail-closed 报错）；三参数进 trades_signature。对照方案 `low_pct_03`/`atr_02` 已入 m2 sweep A 组方案表（归出场类、标 R 口径变），**待目标机跑批定取舍**（本机无 vipdoc/S_DATA） | tick 固定金额让同一参数在不同价位股票上是不同风险量，且实测效果对 buffer 大小敏感、3→5 即反转——余量应按风险单位而非价位数 | 目标机同批对照（tick_3 vs low_pct_03 vs atr_02，钉死口径）期望%/margin 对比；全量测试与静态检查不回归 |
| 2026-08-12 | v0.47 | **#45②：`as_of` 补进 `market_breadth`/`sentiment`/`turnover` 三段契约**（required+nullable，与 `overseas_market`/`amv_0` 同形；「编一个 as_of 等于给门控假新鲜度」——null 合法、缺键硬失败）。前置普查：三个生产者（collector 骨架初值 None、merge_incremental、refresh_market_indices）本就键恒在 ⇒ 先生产者后契约的顺序天然满足（#52 教训），契约补齐零生产改动。`is_stale` 的 as_of 分支（判据②，as_of≠当日⇒陈旧）此前已存在，契约保证键存在后它才真正可靠（此前段无此键时静默落空、只按 quality 判）；`day=None`/`as_of=None` 刻意不修边界归入代码 docstring。行为变化仅限：生产者落盘时三段缺 as_of 键将 require 硬失败（现状无此路径）。**#13：`bottom_surge` gate 语义修正**（owner 裁决「异动后的 J<13 触发，不一定是首次，可以多关注几次」）——新 gate `bottom_surge_j13`/`bottom_surge_strict_j13`：异动后 60 天窗口内**每次 J<13 都触发**（J 阈取 `b1_thresholds`，与 j_low 同口径），修掉旧口径「异动后 60 天持续为真 ⇒ 非事件日持续触发」（R7:141，首轮 107,831 条几乎无选择性）；旧 gate 原样保留作对照。新 gate 语义与 `surge_then_b1`/`surge_strict_then_b1` 一致，独立命名位供 R7 命令槽位与新旧对照；**待目标机回测**（本机无 vipdoc） | TODO #45 第二步与 #13 均 owner 拍板；前者契约只管结构不管新鲜度的边界此前靠生产者自觉（键缺失时 scorer 的 as_of 判定静默失效），后者旧 gate 的 60 天持续为真让信号失去事件性 | 三段产出 as_of 键存在性由测试钉住；新 gate 触发语义（当日不触/窗口内多次/窗外不触/严变体）由测试钉住；全量测试与静态检查不回归 |
| 2026-08-12 | v0.48 | **#51 冷却机制落地（合并原 #31+#51②，owner 定「同一机制家族、落复盘报告节、只提示不拦截」）**：新增 `close_review/cooldowns.py`——① **止损冷却名单**：`match_status=="full"` 且 `pnl_pct <= −7%`（与 `weekly_review.STOP_LOSS_PCT` 同阈，测试钉住）的平仓票进冷却，冷却期 **10 个交易日**（`system_principles` 用户画像既有约定；交易日历数不出截止日时保守视为仍在冷却并标注），每日 `final_close_review`（附「当日在持」交集提示）、每周 `weekly_review`、每月 `monthly_review` 各出一节；② **胜率降仓提示**：月报正式一节输出当月胜率与 35% 阈值判定（`monthly_review` 原 notes 雏形升级为正式节）。**只提示不拦截**：自动链没有买入/仓位决策可拦（`buy_actions` 字面量空表）。README 决策优先级段补写这一事实；`system_principles`/`TRADE_LESSONS`/contracts 两文档的「零实现，见待办 #51」全部改指实现。行为变化仅限三份复盘报告各多一节 | 「触发止损的票进冷却」「月胜率<35% 降仓」两条规则长期零实现，而文档把它们写成硬规则——比没写更危险；落复盘节是 owner 定的落点 | 三份复盘报告出现新节且无命中不静默；cooldowns 无拦截语义由守卫钉住；全量测试与静态检查不回归 |
| 2026-08-12 | v0.49 | **#32 reconcile_positions 可诊断性梳理**（owner「还需要优化和梳理」）：① 超卖报错带**分叉点**（`compute_positions` 的 ValueError 现含代码/日期/时间/卖出量/当时持仓——单点修改，对账与导入两路径同受益）；② 未给 baseline 时超卖报错直接附 `--baseline` **格式示例**；baseline 文件缺失/非法 JSON/缺字段均 SystemExit + 格式引导（此前抛裸 traceback）；③ 17:00 链在 mismatch/replay_failed 时 stderr 打 `[WARN]`——此前非阻断路径退出码为 0、结论只躺 run log 的 note 里，等于静默。行为语义零变化（不阻断、状态分档、指纹均未动）；剩余「观察后转 strict」留 TODO #32 | 失败只给一个状态串时，排错要从头回放；baseline 的存在与格式靠猜；非阻断的失败无人看见 | 超卖报错含分叉点、baseline 引导、stderr WARN 均由测试钉住；全量测试与静态检查不回归 |
| 2026-08-13 | v0.50 | **1800 选股流程重构·阶段 A（止血）（#37，owner 四项定案全按推荐项）**：① **s_shape 完全移出分层**——`technical_score` 的 s_shape 主路径（S**+sstar_level 65/40）删除，技术分/层级统一为 patterns 累加路径（60/30，两套阈值并立消解）；s_shape 降为展示/证据列（s_star/s_shape 仍落盘、candidate_table 保留 S** 列），`live_use` 改 evidence_only，`KNOWN_STATUS_USE_CONFLICTS` 清空（ratchet 测试改为「必须为空」）。② **板块分移出总分**——总分=技术分（原 0.6×技术+0.4×板块+共振±5 废）；板块/共振仍落盘展示；`sector_phase.favorable` 移出「可买=A+四面共振」定义（可买=A+市场/基本面/技术三面共振），四面共振降级为情境标注列；sector_phase 的 live_use 订正为 evidence_only（自 v0.25 起就只是 hint）、_note 的「熊市减亏 4-6pp」旧结论改记 R4 重跑未复现。③ 证伪/证据因子下线：`perfect_b1_fit` 停加分（evidence_only 不加分，贴合列仍展示）、`b1_pullback_fit` 停止逐票计算（证伪；落盘字段留不可用标记，因子转 debug/none）。④ 死代码与漂移：SECTOR_STATE_MAP 删无人消费的 cap 死列、sector_heat reason 文案与「板块不封顶」行为对齐、`next_step` 的 `generate_buy_plan`（虚假承诺，BuyPlan 契约已删、零行动读者）改名 `buy_review`、SCREENING_WORKFLOW 打分一节重写为统一口径。⚠️ live 行为变更：同日同候选下 A/B/C/D 分层与总分可能变化（s_shape 65/40 → patterns 60/30、板块权重移除）；阶段 B（adx25/s_reversal/观察区）与 C（日报展示口径）不在本次范围 | R2 已证 S_shape 无 alpha 而 live 拿它分层（研究-live 矛盾）；板块封顶 v0.24 已废但权重/可买定义里还残留板块驱动；证伪因子仍在逐票烧计算 | 18:00 链候选分层/总分分布变化（预期）；candidate_table 可买档不再含板块腿；全量测试与静态检查不回归 |
| 2026-08-13 | v0.51 | **1800 重构·阶段 B（接入验证过的，证据层）（#37，owner 批准）**：① **adx25 证据列**——R2:67-72「J<13 且 ADX≥25」是研究链首个跨牛熊一致改善的入场过滤（三窗全改善），R2 裁决「证据层接入观察一季，勿直接进 live gate」；候选落 `adx`（数值，`indicators.dmi_arrays` Wilder 唯一实现）与 `adx25`（布尔，J 阈同 j_low）。② **s_reversal 落盘**（原 enrich TODO ② 闭环）：`s_shape.compute_s_reversal`（买弱/反转分）此前存在但 live 从不调用 ⇒ 接进证据列；成本与 compute_s_shape 同阶（纯内存 O(n)），不设开关。③ **门槛外观察区**：J<13 硬门槛不动（R1 框架），被挡但异动强的票（初版判据：底部巨量 `bottom_volume.hit` 或放量点火 `ignition.hit`，注释写明是初版）落 `watchlist_outside_gate`，candidate_table 新增「👀 门槛外观察区」节——不进主池/分层。三者均为**严格证据层**：不进技术分/分层/gate（有防回归测试钉住分层与总分逐位不变）；stock_pool 契约加可选字段（adx25/s_reversal/watchlist_outside_gate，required=False 跟随证据字段惯例）。candidate_table 主表加 ADX25/S反转 两列 | R2 验证过的入场过滤此前只在研究侧；s_reversal 存在但 live 看不到；J<13 挡掉的异动票完全不可见——三者都先作证据观察，再谈是否值正式通道 | 候选 JSON 出现新字段、候选表出现新列/新节；分层与总分逐位不变（防回归测试钉住）；全量测试与静态检查不回归 |
| 2026-08-13 | v0.52 | **1800 选股流程重构三阶段收口（#37，阶段 C：日报/候选表展示口径）**：① 日报「公式选股备选池」节——**全量 A/B 展示**（原 `bucket in ("A","B")][:10` 截断删除：C/D 不进日报已使可见性=分层过滤，再截 10 只是任取）+ 证据列（ADX25/S**/资金意图层级，0AMV 状态入状态行）+ 排序语义如实标注（「分档=形态分层、表内顺序为未校准启发式、不是 alpha 排序」，不出现「优质/推荐」类语义）+ 回退旧期时打**过期警示**（「候选池为 X 日旧数据」，盘点报告第 9 条顺势收掉）。候选节抽成 `daily_report.stock_pool_section` 供单测。② candidate_table Top5 保持存在，加排序口径标注（总分=技术分、未校准启发式、非 alpha 排序；S** 为展示列）。③ 文档：SCREENING_WORKFLOW [4] 节更新。三阶段总账（v0.50/v0.51/v0.52）：s_shape 移出分层、板块分移出总分、四面共振降级为情境标注、证伪因子下线、adx25+s_reversal 证据层、门槛外观察区、日报全量 A/B+证据列+过期警示 | C/D 档不进日报 + top-10 截断 ⇒ A/B 过滤事实上决定人看见谁，而排序是未校准启发式——展示口径必须把这一点写给读者 | 日报候选节全量 A/B 可见、证据列在、旧数据有警示；Top5 带口径标注；全量测试与静态检查不回归 |
| 2026-08-13 | v0.53 | **目标机 review 缺陷修复 + 三组跑批落档**。修复：`cooldowns` 台账坏行防御×2（非 ISO 日期炸三份复盘 ⇒ 跳过并计数上报；NaN pnl 误入冷却名单 ⇒ 排除并计数上报）、`monthly_review` 降仓阈值字面量 35 改引 `cooldowns.WIN_RATE_REDUCE_THRESHOLD_PCT`（同源钉）、治理文档旧平铺报告路径残留改日期目录口径（MASTER_WORKFLOW/DATA_FLOW_CONTRACT）。落档：**#8（R3）** 6 个受影响窗已用 tdx 前复权 + vipdoc 全宇宙（5,542 只）重算——「≥100% 带 −37.5%」作废（合并口径 +19.9%，逐窗 4 负 2 正、靠 2023-09 一窗拉动），「召回单调下降」软化为「方向窗间不稳定」；**#13（R7）** 严变体 `bottom_surge_strict_j13` 首轮有戏（110 条、H10 胜率 61.9%、H20 +2.43%），宽变体仍弱，seed/区间复核待跑；**#25（R10）** 0AMV×出场组合两案——`pct_05_amv_cz3`（+10.5pp/+0.287R）优于 `pct_05_amv`（5% 档首选换位），trail_08 叠 amv 无协同；cross-window 复核待跑 | 目标机 review + 三组跑批结果回收 | 复盘报告不再被台账坏行炸掉；复核跑批按 TODO #13/#25 命令执行 |
| 2026-08-13 | v0.54 | **出货形态三项补全（25chuhuo 讲义覆盖度缺口，owner 批准）**：① **次日确认豁免层**（`distribution.confirm_distribution`）：①/②（天量大阴/次高巨量长阴）命中后，信号 K 次日未破其低点或反包（收复实体上沿）⇒ revoked（换庄/试盘/假出货豁免——中国中铁 14.12.22/15.6.8）；T+1 未收盘 ⇒ pending；否则 confirmed。**不改 detect_distribution 既有命中语义**（有回归钉）；enrich 落 `distribution_confirm` 证据列（不进分层）。② **③ 阶梯跌破补全**：连续阴计数含**平量阴**（放量/平量分拆计数供消融）、新增 **DKS（黄线）跌破**路径（平量也判；QSX 白线路径仍需放量）——`stairstep_vol_decline` 命中条件放宽，detail 键改名（consecutive_bears/of_which_vol_up/below）。③ **顶部大风车**（`detect_top_windmill`）：高位（同⑤口径）+ 长上影/宽幅震荡 K + 次日不反包确认，自带 T+1 状态机；只产信号与状态，仓位动作归文档/人。新阈值常量（DIST_WINDMILL_SHADOW_BODY=1.0 / DIST_WINDMILL_RANGE_PCT=7.0 / DIST_WINDMILL_TOP_FRAC=0.98）均标待回测 | 25 种出货形态覆盖度分析：豁免（换庄/假出货误杀）、③ 平量阴/DKS 缺口、大风车三处讲义有而代码无 | 豁免撤销路径与③放宽由合成用例钉住；既有五信号回归不动；全量测试与静态检查不回归 |
| 2026-08-13 | v0.55 | **优秀 B1 指纹证据层回测（R18，新研究单元）**：`research/b1_fingerprint_study.py`（--b1-data-dir 参数化，滑窗 as-of 逐日调 `compute_metrics`，检测逻辑零重写）对 B1_DATA 10 只正例跑召回：合成标签 `b1_ignition` 召回 2/10（4 个命中日，驱动腿均 j_low+回调缩量+放量点火三腿齐）；单项腿召回 j_low 10/10、回调缩量 8/10、放量点火 9/10、反转K 4/10、知行近金叉 0/10（样本 61~78 根 < DKS 所需 114 根，结构性不可得）；命中集中样本末端 ⇒ 后续收益窗口不可得（n=0 如实报告）。**样本边界**：精选正例无负例 ⇒ 只回答召回与命中后走势，不回答胜率/判别力（写进输出与 R18 头部） | good_b1 图集从描述层落证据层；合成标签在短样本上合取过紧是可观察事实 | owner 补样本（更长历史+对照票）后同脚本重跑可升级证据等级；全量测试与静态检查不回归 |
| 2026-08-13 | v0.56 | **底部识别形态两检测器（25chuhuo 讲义底部侧，owner 指示）**：新因子 `factors/bottom_patterns.py`（L2，仅依赖 L0 indicators）——① **W 底**（`detect_w_bottom`）：双底（镜像双顶分型/容差 3%/间隔≥3）+ 底部区域（距 250 日高回撤≥40%）+ 底部放量（量≥250 日均×2 且不创新低）+ MACD 底背离（收盘摆低 L2<L1 且 DIF 抬高，口径同 enrich bottom_divergence，L0 macd_series 自重算）四腿合成、逐腿落 detail 供消融；② **红肥绿瘦**（`detect_red_fat_green_thin`）：底部区间阳线占优两维都要（数量阳多阴少 + 面积阳实体/阳量 > 阴实体/阴量），镜像顶部绿肥红瘦。严格证据层（evidence_only）：enrich 落 `w_bottom`/`red_fat_green_thin` 证据字段、score_candidates 白名单透传、candidate_table 加两列（主表 24 列）。阈值全标待回测。B1_DATA 10 票实测：红肥绿瘦 9/10 有命中日（集中 6 月中~7 月初的底部段）；W 底合成 0/10——双底腿 4/10 有，但「底部区域」腿（250 日窗）与「MACD 底背离」腿在这批短样本上结构性缺（样本 61~78 根且底部多为等低双底/V 形，无背离），如实记录 | 讲义底部侧口径此前零实现；出货侧（v0.54）已有镜像参照 | 合成用例钉四腿合成与缺腿不命中；B1_DATA 实测结果见 commit 与 R 记录；全量测试与静态检查不回归 |
| 2026-08-13 | v0.57 | **三份报告角色对齐（owner 定版）**：盘后（final_close_review）=复盘纠错+**条件化预案主产地**——§2 新闻舆情压缩为「与今日操作相关的事实核对」（只留与当日持仓/成交交集的条目，无交集/无证据如实说），§4 主线题材压缩（≤5 行）并标注「判定口径待重设计（#26）」，§6 节首注明「明日预案以本节为准，盘前只做确认与信息刷新」；盘前（daily_report）=信息处理+**预案确认**——§6 改「预案确认」节（读前一交易日 final_review 的 next_day_plan 逐条确认/标变化，盘后预案缺失如实报不编），§5 主线标注待重设计；盘中 1445 现状吻合不动。三份报告标题区各加角色行一眼可辨；DAILY_PLAN_TEMPLATE/MASTER_WORKFLOW/README 职责描述同步；TODO #26 补入主线题材重设计 | 三份报告角色此前含糊：盘后塞信息流、盘前另出计划——同一事实两处生产必漂移 | 盘后 §2 只含交集事实；盘前预案确认节读盘后预案、缺失如实报；全量测试与静态检查不回归 |
| 2026-08-14 | v0.58 | **技术分权重 owner 逐条定案 + 首个负分项**：① **降权**：bbi_above 25→**5**（J<13 常伴短暂破 BBI，+25 惩罚的正是 B1 形态）、反转K 25→**4** 且**不再取代** j_low/缩量子项（R18：优秀 B1 里反转K 仅 4/10，非必要条件）；删 j_mid 死分支（主池恒 J<13，13≤J<50 永远走不到）。② **调/新增**：底部巨量 6→10（缩量维持 15——owner 复核后不改）、周线 J<13（周日共振）+5、ADX>60 +5、知行位置三态（QSX>DKS 多头 +6 不变；骑线 C≥QSX +4；回踩区 QSX>C≥DKS +2——`zhixing_state` 补 `close_above_dks` 字段）、近 10 日阳量>阴量 +5 / 阴量>阳量 **−5**（新因子 `bottom_patterns.bull_bear_volume`，中性窗口纯量能对比，enrich 落 `volume_yy`）、出货形态分数层减分 watch −10 / high −20（**封顶规则不动**，cap_rules 只管封顶通道）。⚠️ 直接后果：patterns 全中也只有 59 分，技术强（≥60）必须叠非 patterns 因子 ⇒ 强/A 档门槛实质抬高，分层分布会变（预期内）。仍是未校准启发式（R2：排序无 alpha），分值只管分层与展示序。配套（同日 candidate_table 三节优化，owner 指示）：主线指纹剔除通达信自定义分组（`sector_mainstream.EXCLUDE_CUSTOM_NAMES`：含B股/含GDR/含H股/含可转债/ST板块/次新股/通达信88）并按候选数排序（`mainline_fingerprint` 新增 `sort_by`，默认 density 不变）；信号标注 SG 行并入 SB（SB=SG∧J<13，本池恒重合，图注说明）；门槛外观察区按日 J 升序 | owner 逐项审定权重：bbi/反转K 加分过多且无组件级回测支撑；知行位置/阴阳量/出货此前不进分数 | 分层分布变化（强档变少预期内）；新加减分项由合成用例钉住；网格/fixture 测试按新权重重写；全量测试与静态检查不回归 |
| 2026-08-14 | v0.59 | **公司「地位」基本面证据层（owner ⑧ 拍板「需要做」）**：新增 `datasource/local_tdx/fetch_company_profile.py`——东财 F10「公司概况」接口（`PageAjax`，与 fetch_market_cap/pit_financials 同体系；通达信本地无 F10 文字，vipdoc 只有行情/财务）批量拉全市场公司简介（ORG_PROFILE）+ 经营范围（BUSINESS_SCOPE），按地位关键词（唯一/龙头/最大/第一/领先/市占率/寡头/垄断/独角兽 等 23 个，长词优先定位代表句）扫描落 `data/fundamentals/company_profile.jsonl`（增量拉取、周期原子落盘、`--rescan` 离线重扫）；enrich 透传 `company_position` 证据字段（available/keywords/snippet/industry_em），score_candidates 白名单透传。**严格证据层**：不进技术分/分层/gate。⚠️ 两条口径自知（写在模块 docstring）：文本是公司自述/第三方整理，非独立事实核查；**非 PIT**（简介可随时被改，无法回溯当时版本）⇒ 只可用于 live/近端，禁止用作历史回测特征 | owner：通过公司介绍中「地位」信息的关键词给基本面一个判断；本地无此数据，需新建采集通道 | 台账覆盖率与命中分布（首轮全市场拉取后复核）；证据字段不加分由测试钉住；全量测试与静态检查不回归 |
| 2026-08-14 | v0.60 | **MACD 加分项 + 量能撤退封顶去除（owner 逐条定）**：① 新增三项 MACD 加分（各 +5）——**水上**（DIF>0 且 DEA>0，白黄线零轴上）、**日线红柱增长**（hist>0 且大于昨值）、**周月红柱同增**（周线与月线重采样后 MACD 柱均在增长；`check_macd_technics` 新增 above_water/bar_red/bar_grow/wm_bar_grow 判定字段，历史不足返回 False）；② **MACD 底背离 5→8**；③ **量能撤退（volume_sustain=retreat）封顶 C 规则去除**——与「缩量回调是 B1 健康形态」语义冲突（同一缩量事实一边加分一边封顶）；检出仍记录 `main_force_retreat_cap_disabled` 证据 flag，DEFAULT_CAP_RULES 与 SCREEN_FORMULA_REGISTRY 同步置 false。⚠️ 003074 式案例：owner 图感「底背离」与检测器口径（两个收盘摆低 + DIF 低点抬高、右确认防未来函数）不一致——002074@2025-08-01 最新低点 DIF 是区间最低，按口径不构成底背离，未为其放宽检测器（阈值松动须回测） | owner 实盘核对：MACD 位置/柱体形态应进分数；量能撤退与缩量回调冲突 | 新加分项由合成用例钉住；撤退不封顶但留痕由测试钉住；全量测试与静态检查不回归 |
| 2026-08-15 | v0.61 | **优秀 B1 正例定向校准（owner 拍板，8 只正例图集）**：目标=owner 提供的 8 只优秀 B1 正例（002074/002812/002940/301076/600184/600366/600601/688321，2025-06~09 各自信号日）在当前打分系统下全部 ≥70 分且 A/B 层（owner 排除 300689/605378 两只）。改动：① **提权**--j_low 20->**33**、知行多头 6->**9**、骑线 4->**5**、回踩区 2->**5**、MACD 水上 5->**7**、回调缩量 3->**5**、修复信号每项 3->**4**（上限 8）、阳量占优 +5->**+7**（阴量 −5 不变，不对称为 owner 定向选择）；② **回调缩量阈值** PULLBACK_VOL_RATIO 0.8->**0.85**（正例 002074 量比 0.849 被 0.8 挡掉）；③ **资金意图新增「回调缩量 +2」**（缩量回调=抛压衰竭主力未撤；正例 002074 资金意图原 0 分被压 D）；④ **MACD 顶背离单独出现不再封顶 C**，改分数层减分 **−8**（risk_flag 留痕；三打白骨精仍封顶）--正例 301076 被顶背离封顶压出 A/B。回放验证：8/8 达标（70/82/91/95/75/100/73/79 分，6A+2B）。⚠️ **方法论自知（同写进代码注释）**：这是对**事后选取的 8 只正例**的定向拟合（R15 幸存者偏差警示）--J<13+知行多头+MACD 水上的共同因子基底抬到 49 分，全池技术档位整体上移、「强」档区分度下降，owner 知情接受（宁可放进观察、不可错杀正例）；后续应按 R18 模式补负例对照做判别力校准，校准后权重可能回调 | owner：优秀 B1 不能被分层埋没（8 只正例此前仅 2 只 A/B，3 只落中档、2 只落弱档、1 只被顶背离封顶） | 8 只正例 as-of 回放全部 ≥70 且 A/B（已验）；v0.58-v0.60 合成用例按新权重重写；全量测试与静态检查不回归；观察若干交易日强档数量变化（预期大幅增多） |
| 2026-08-15 | v0.62 | **顶部大风车校准+接线（owner 定向，北方铜业 000737 案例）**：owner 发现 000737 在历史高点区（2026-08-10，60 日顶 15.54 的 94.07% 次高位）出现「长上影 1.75×实体 + 长下影 4.75×实体 + 量 1.69×常态」的天量大风车，但系统在 QD 多头名单里**零出货标志**。排查三因并修：① **高位阈值** `DIST_WINDMILL_TOP_FRAC` 0.98->**0.93**--0.98 只认「贴着最高点」，次高位（顶后反弹 ~94%）大风车全部漏掉；② **新增天量腿** `DIST_WINDMILL_VOL_RATIO=1.5`（量 ≥ 前 20 根**中位量**×1.5，owner 口径「长上影和长下影的历史天量大风车」；基线用中位数--拉升段放量会把均值基线抬高错杀真天量，08-10 实测均值比 1.477 vs 中位数比 1.691）；③ **接线**：大风车从纯证据层（v0.54 只落 confirm_distribution）进 `detect_distribution` 第 ⑥ 出货信号--**仅 confirmed 计 hit**（pending 待确认/revoked 反包豁免不计，T+1 纪律），单独命中 watch 级（封顶 C + 技术分 −10 经既有通道自动生效），hits≥2 -> high 既有规则覆盖。验证：000737@2026-08-14 回放 windmill confirmed、hits=['top_windmill']、risk=watch | owner：「有这种形态应该是顶部出货」--大风车是主力对倒出货形态，此前形态漏检+证据层不拦截双重失效 | 000737 回放命中（已验）；次高位/天量腿/接线/pending 不计各由合成用例钉住；观察大风车命中面（0.93+1.5×放宽的影响）若干交易日；全量测试与静态检查不回归 |
| 2026-08-16 | v0.63 | **技术分去膨胀（owner 定向回调）：j_low 33 -> 24（单项）**。v0.61 上线后 owner 复盘 08-14 池发现 70+ 达 **67 只**（915 池 7.3%）判定膨胀--根因是 j_low 33 单项占满分 1/3，共同基底（J<13+知行多头+MACD 水上=49 分）使「过门槛即高分」。用 08-14 池 factor_contrib 离线模拟五组降膨胀向量后定案：**只回调 j_low 到 24**（v0.60 原值 20 与 v0.61 的 33 之间偏保守位），70+ 预期 67->**26 只**（−61%）。关键无损性验证：8 只正例技术分虽降 9 分，但**全部仍 ≥60「强」档**--002074=61、600184=66、600601=64 跌破 70 分线但 A/B **桶位一只不丢**（002074=B、600184=A、600601=B）；「不被埋没」由分层承担，70+ 分数线回归稀缺信号 | owner：「70 以上的太多了」--分数线失去稀缺性即失去信息量 | 08-14 池重跑后 70+ 实测 ≈26（离线模拟已验）；正例桶位不变；v0.61 合成用例按 j_low 24 重写；全量测试与静态检查不回归 |
| 2026-08-16 | v0.64 | **B1 健康回调组合包 +9（owner 选定 V6a）**：J 低位 ∧ 缩量回调持 DKS（pullback_shrink）∧ 知行多头（QSX>DKS）**三腿齐备**发组合奖 `b1_healthy_pullback_pack`。设计动机：owner 追问「能否正例上 70 但不膨胀其他人」--离线模拟证实完全零泄漏不存在（正例与 60-69 分人群单因子同质：人群 84% 有 rs、91% 水上），**最小泄漏 = 组合奖**（只奖励完整结构）：8 只正例 7/8 三腿齐（002812 无缩量回调不吃包、73 分已够）全部回到 ≥70；08-14 池仅 ~1/3 命中，70+ 预期 26->**38**（仍比 v0.61 普涨的 67 少 43%）。曾评估 V6b（回调中阴量豁免）因池 70+ 达 43 未取 | owner 拍板选 V6a：分数线稀缺与正例可见性的平衡点 | 8 只正例回放 8/8 ≥70；三腿合成/缺腿不发由合成用例钉住；08-14 池重跑后 70+ 实测 ≈38（对照模拟）；全量测试与静态检查不回归 |
| 2026-08-16 | v0.65 | **候选表两节可读性优化（owner 指示）**：① **主线指纹剔抽象概念**--新增 `sector_mainstream.EXCLUDE_ABSTRACT_CONCEPTS`（43 名，三类：政策/区域叙事如一带一路/雄安/自贸/乡村振兴；指数外资持仓如 MSCI/QFII/北上/各类重仓；股本结构与公司行为如低价/破净/送转/次新/回购/增减持），与既有 EXCLUDE_CUSTOM_NAMES 合并为 `EXCLUDE_MAINLINE_JUNK` 供 candidate_table 剔除；判据=板块名描述的不是公司经营的产品或服务（「网络规划设计」是行业概念保留）。② **信号标注一览改表格**--从项目符号列表改为「因子｜命中/可评｜命中候选」三列表，命中名单按技术分降序取前 12 并**括号标注各票技术分**（总分=技术分），一眼看出命中的是强票还是弱票 | owner：「去掉一带一路这类和主营业务无关的抽象概念」「改表格同时标技术分提升可读性」 | 08-14 报告重跑后主线榜头名从一带一路（210 只）变为人工智能；信号标注节为表格且带分；全量测试与静态检查不回归 |
| 2026-08-16 | v0.66 | **复审缺陷批量修复（三轮 review 发现，全修）**：① **wm_bar_grow 死字段复活**——月线 EMA26 需 ≥40 根月线而生产 df 恒 260 根日线（~13 根月线）⇒ 周线/月线红柱 +5 从未发出过；`check_macd_technics` 加 `df_long` 参数，live 链逐票加裁 1200 根长历史（`OHLCV_LOAD_BARS_LONG`），研究/注入路径回退 df 自身并新增 `wm_available` 如实区分「算不出」与「没增长」；周/月末半成品 bin 口径写入 docstring。② **技术分边界**：负分补下限（min-max 0-100，负分明细仍落 factor_contrib）；骑线 +5 补 `qsx_gt_dks` 前提（空头排列价站 QSX 不再加分）；阴阳量平局不加不减（改按总量比较，旧形状兜底）；出货减分加 `available` 守卫（未评估的残留 risk_level 不减分）。③ **W 底选点**：左底取「与右底价位最接近」的前谷（原取最浅谷，教科书 W 底 3% 容差腿假阴性）；「250 日」窗口 n<250 时落 `window_bars` 如实标注；红肥绿瘦 n<60 时 `near_bottom_degenerated` 标注。④ **工程加固**：fetch_company_profile 失败记录自动重试（原永久占位）；company_position 缓存加载失败打 WARN + conftest 钉空防机器相关断言；launch_point_study `--codes-file` 加固（BOM/注释/6 位校验/存在性/互斥报错，抽 `_read_codes_file` 可测）；cooldowns 日期锁死扩展 ISO 格式（3.11+ fromisoformat 接受「20260803」基本格式会混进字符串比较）。⑤ 文档/注释：R7 基准 seed 标注改 R6:294（seed=1）；v0.65 抽象概念数 44→43；distribution 天量腿注释「均量」改「中位数」（与 v0.62 实现一致）；score_candidates 顶背离注释跟上 v0.61 减分口径；b1_fingerprint_study 中位数改 statistics.median（原偶数取上中位）、code 前导零保留 | review 发现的问题全修（ owner「把发现的问题都修了」） | 每项有对应测试钉；全量测试与静态检查不回归 |
| 2026-08-18 | v0.67 | **#49 持仓快照历史归档落地**：新增 `core/positions_history.py`（L0）——写方两个快照导入点（`incremental_ledger` 增量导入含 `--confirm-no-trades`/空增量、`standardize_trades` 全量灾备导入）在写入 `current_positions.json` 后同步归档到 `data/trades/positions_history/{date}.json`（temp+os.replace）；读语义「≤ date 的最近一份」（持仓不变的日子不产生归档）。`rss_filter.entities(date)` 的 `date` 形参**从此生效**：回填历史日期用那天的持仓筛那天的新闻，不再用今天快照；归档尚未积累时回退当前快照并在 filter 报告落痕 `positions_source`（`history:{date}` / `history:{date}(≤{date})` / `current_snapshot`）。⚠️ 历史从部署日起积累，此前的日期仍只能回退当前快照 | `entities(date)` 形参长期未使用，回填历史日期结论不可复现（TODO #49 ⏸ 项）；归档机制是数据层前置，机制落地后 live 每日自然积累 | 归档/读回退语义由合成用例钉住（精确/最近一份/未来归档不可用/无归档回退）；两写入点归档由测试钉住；全量测试与静态检查不回归 |
| 2026-08-18 | v0.68 | **#58 研究侧高复杂度函数主动拆分 + 逐位等价效率优化（owner 指示，覆盖原「不为拆分而拆分」原则）**：研究侧头部 F/E 级全部降到 ≤C——`launch_point_study` 8 函数（main F125→B6 变纯调度 + `_mode_*` 五模式、discriminate_at_signal F105→C12、extract_firings F61→B10、aggregate_discriminate F56→C11 及四个 E 级报告函数）、`backtest_factors` 4 函数（main F80→B6 `_build_parser`/`_run_trade_sim`/`_run_signal_scan` 纯调度、simulate_b1_trade F62→C18 抽 `_initial_stop`/`_update_dynamic_stop`/`_stop_hit`/`_scale_out_triggered`/`_cost_zone_flat` 五个纯判断、evaluate_trades E31→C20、simulate_portfolio_topn E31→C17 并与 simulate_portfolio 收敛共享 `_close_positions_until`/`_curve_cagr`）、`m2_stop_sweep._print_trade_group` F69→B9（`_judge_row` 判定三分支纯函数化 + 三处横幅收敛 `_banner`）、`reconcile_qfq` 四函数 E/D→≤B7（`_merged_pair`/`_xdxr_event_dates` 收敛三处复制）、`analyze_winner_features.main` F69→C17（⚠️ `rows.sort`/`win_half` 两行按源码字面断言契约留在 main 壳）。**效率优化全部逐位等价**：① evaluate_trades 的 entry_gate 每 bar 重算 KDJ/MACD 全前缀（O(n²)，实测占回测 99% 时间）改逐股预计算全序列按索引取值（ewm/rolling/Wilder 从第 0 根递归 ⇒ 数学逐位等价；gate 改 `(df, precomputed=None)` 双形态不改 SCORERS 签名；`evaluate()` 的 gate_window>0 路径起点非零不适用、未动）——合成基准实测 **19.7×**；② extract_firings 的 BBI 逐信号重算改逐股预计算；③ 报表侧 hoist/去重若干。等价性验证：新增 `test_gate_precompute_equivalence.py` 106 例（26 gate × 参数变体逐笔 ==）+ 新旧版合成数据全路径输出 `==` 比对 | TODO #58 收口；回测引擎可维护性与耗时是研究迭代的最大摩擦点；签名锁/stdout 契约/退出码/trades_signature 全程未动 | 新增等价钉测 106 例；全量 4147 passed（+106）；audit 套件通过（ruff format ✓ / mypy 0 errors）；radon_cc.txt 当日重生成确认对象函数 ≤C；⚠️ live 链 F 级函数（enrich/detect_distribution/technical_score 等 ~20 个）未动，恢复原原则 |
| 2026-08-18 | v0.69 | **#58 收口：live 链 F 级函数全部拆分（owner 指示「都拆了」），radon F 级清零**。23 个 F 级 → 全 ≤C：选股链 `enrich` 86→C15（八段装配）、`compute_metrics` 48→A1、`technical_score` 69→A4（八段评分）、`score_candidate` 43→B9、`screen_formulas` 42→B8；因子层 `detect_distribution` 77→C13（六条出货腿各自成函数）、`compute_s_reversal` 57→B7；持仓链 `_row_from_analysis` 66→A3、`b1_holding_state.evaluate` 61→C17（九组信号判定）；复盘链 `build_weekly_review` 57→A3 / `render_markdown` 55→A1 / `holding_week_performance` 41→B6 / `build_monthly_review` 74→A1（逐节取数+渲染）、`review_core.main` 54→B10 / `final_close_review.main` 52→C14 / `execution_review.main` 41→A1；数据层 `refresh_market_indices.main` 50→B7、`rss_filter.main` 48→B6（`_score_item` 逐条评分抽出）、`fetch_market_cap.main` 42→A5；择时/日报 `price_volume_state` 41→B8、`chief_decision_report.main` 43→C14、`theme_tracker_report.make_report` 41→C11、`daily_report.main` 44→B7。**行为零变化**：周报/月报拆分前后 SHA-256 逐字节比对 MATCH；签名/字段键序/文案/退出码/monkeypatch 通道全程未动。⚠️ 过程中发现四个 `inspect.getsource` 源码字面钉板（`analyze_winner_features.main`、`final_close_review.main` 角色行、`chief_decision_report.main` 门控块、`daily_report.main` 角色行与 #26 标注块），相关行留在 main 壳并加注释；`test_enrich_b1cz` 一处 AST 钉板改为沿模块内调用链递归收集（比原断言更强）。两处 mypy 回归（execution_review 注解、daily_report Optional 键）已修 | 同 v0.68——可维护性是迭代摩擦点；live 链比研究侧风险高，靠既有 4000+ 测试与逐字节基线兜底 | 全量 4147 passed；audit 套件通过（ruff format ✓ / mypy 0 errors）；radon_cc.txt 重生成确认 F 级清零；D/C 级恢复原原则（因业务动时再拆） |
| 2026-08-18 | v0.70 | **#35 三因子降级「不再研究」（owner 拍板）**：`alpha_pvcorr` / `low_vol` / `momentum` 的 `status` 由 untested → needs_work。证据：钉死同一宇宙（vipdoc 5543 只 seed=0 抽 300），3 窗口 × 4 打分器 top-5 择优对照（j_low、25bps，`artifacts/logs/w35_*`）——rally22：baseline +3.8% 对三因子全负；bull2425：alpha_pvcorr +42.8% / low_vol +14.0% 跑赢、momentum −23.6%；ytd26：全员亏损、momentum −7.3% 相对最好。**三者排序 edge 各只出现在单一 regime，无跨窗口稳健性**（差异全在 top-N 排序，全候选池统计各 scorer 一致） | 三因子此前标 untested 缺独立证据；补跑后证实 R2「选股无 alpha」整体结论对它们同样成立，edge 集中单一 regime 重蹈 R10 教训 | 因子注册表测试通过；R2 结论表与第 16 条同步；三者 `live_use=none` 不变，不进 live 链 |
| 2026-08-19 | v0.71 | **#58 尾巴收口：radon E 级清零（owner 拍板范围=只拆 E 级）**。19 个 E 级全拆到 ≤C：选股链 `apply_risk_downgrades` 39→A2（13 条封顶判据各成 `_cap_*`）、`score_all` 39→C12、`build_entry_reasons` 32→A1、`check_macd_technics` 39→A4（区间状态机/摆动分型/顶底背离/三打白骨精/拐离/周月红柱各成函数）、`check_non_one_wave` 33→B10、`_bucket_pools` 34→A4；因子/基建 `detect_wave_type` 31、`sector_mainstream.aggregate` 40、`market_quality_gate` 37→B10、`load_bars_qlib` 31→B7；数据层 `verify_ledger` 32、`resolve_names_for` 32；复盘/持仓 `review_core.classify` 32→B7（五组信号判定各成函数）、`review_enrichment.main` 40、`calc_mfe_mae.main` 32→B7、`holding_sector_mapper.main` 32、`market_timing_scorer.score_indices` 35；研究侧 `run_bear_to_long_study` main 38→B9 + survivorship_report 37→A5。同批（前轮未提交工作）D 级 53→26。**过程中修一个真回归**：前轮拆分在 `main_rally_factor` 留下 4 个「调用了但从未定义」的 helper（`_flow_cross_state`/`_rsi_oversold_state`/`_t1_state`/`_main_rally_report`），NameError 被该函数「绝不 raise」的 except 吞掉 ⇒ `main_rally` scorer 静默零产出而测试只挡 mcap——按原内联逻辑逐位补齐定义，`test_scorers_registry_all_run` 恢复。另修 3 处 mypy 回归（review_enrichment counts 注解、market_timing_scorer `_score_one_index` 返回注解）。**行为零变化**：周报（2026W33）/月报（2026-07）HEAD vs 拆分后逐字节比对（屏蔽生成时间戳与 base 路径）双 MATCH；签名/字段键序/文案/退出码/monkeypatch 通道未动 | 同 v0.68/v0.69——E 级是可维护性最后一档硬债；「except 吞 NameError」类静默失效只有全量测试能兜底 | 全量 4147 passed；audit 套件通过（ruff format ✓ / mypy 0 errors）；radon_cc.txt 重生成确认 E/F 清零；剩余 D 级 26 / C 级 276 恢复「因业务动文件时先拆」原则 |
| 2026-08-19 | v0.72 | **#58 D 级清零（owner 指示「继续 D 级别的清零」）**：26 个 D 级全拆到 ≤C——研究侧 `summarize_trades` 26→A2/`summarize` 24→A1、`launch_point_study` 三函数（sector_concentration 28/capture_rank_study 25/explain_aggregate 24 全→A5）、`run_backtest` 23→A5；编排层 `run_1700.main` 28→A4、`run_1800.main` 21→A5（⚠️ runtime_gate 段被 `inspect.getsource(main)` 钉板留在 main 壳并加注释）、`daily_pipeline.main` 23→A3（七段落函数）；选股链 `enrich_candidates` 四函数（load_fund_flow 26→A3/check_repair_signals 25→B9/build_stock_theme_map 24→A5/fund_flow_of 22→B7）、`_signal_labels_section` 25→B8（主函数接到已有但悬空的 `_signal_label_row`/`_signal_labels_unavailable_note` 上——前轮事故的另一半形态：helper 定义了却没被调用）、`financial_factor` 28→B10；择时 `n_structure_state` 28/`descending_n_structure_state` 26/`trend_state` 24 全→B（收盘分型扫描与破位新鲜度收敛为两函数共用 helper）、`merge_incremental_market.main` 26→A3（源码切片钉板以 helper 参数命名 `prior_effective_state` 兼容）；数据层 `get_ohlcv_table` 22→A5（签名默认值钉板保留）、`tq_http.call` 22→A5、`fetch_sector_index_history.main` 23→A2、`postclose_news_digest.main` 24→A4；复盘 `weekly_review.portfolio_trajectory` 28（三段落函数）、`calc_mfe_mae.resolve_open_entry_dates` 23（FIFO 回放两段抽出）；基建 `write_runtime_gate` 21。**行为零变化**：周报（2026W33）/月报（2026-07）HEAD vs 拆分后 md+json 四件逐字节比对（屏蔽生成时间戳与 base 路径）全 MATCH；签名/字段键序/文案/退出码/monkeypatch 通道未动 | 同前——D 级清零后全仓仅剩 C 级（287 个，含拆出的新 helper），「因业务动文件时先拆」原则收敛到 C 级一档 | 全量 4147 passed；audit 套件通过（ruff format ✓ / mypy 0 errors）；radon_cc.txt 重生成确认 D/E/F 清零 |
| 2026-08-19 | v0.73 | **#58 C19-20 批次：29 个「准 D」拆分 + 五项逐位等价性能优化（owner 指示）**。拆分：C20×14+C19×15 全到 ≤C15（多数 A/B）——研究侧 `reconcile_qfq.report`/`evaluate_trades`→B10/`adjust_diagnostic` 两 cmd/`scan_signals_ytd.main`/`probe_data_sources.report`/`m2_stop_sweep._collect`/`_eval_feature`；选股链 `check_volume_sustain`/`_base_scalars`/`_load_enrich_context`/`_signal_overview`/`render_table`/`_gate_advisory_section`/`capital_intent_strength`；择时/日报 `build_sector_summary`/`_resolve_holding_actions`/`amv_state.compute`；复盘 `execution_review.build_review`/`_holding_rows`/`_bear_regime_stats`/`_month_pnl_totals`；数据层 `list_universe`/`fetch_period`/`fetch_trade_date`/`_to_close_frame`/`_collect_indices`；因子 `mainline_fingerprint`/`rsi_regime`。**性能优化（全部逐位等价 + 钉测）**：① `mainline_fingerprint` 循环内每板块重复读盘解析 tdxzs.cfg ⇒ hoist 一次加载，合成基准 **69.8×**（237.9→3.4ms）；② `scan_signals_ytd._parse_day` 备忘（信号日高度重复），**15.7×**（716→46ms）；③ `simulate_b1_trade` 每笔四次 `.astype(float).values` ⇒ 逐股 hoist 复用（`ohlc=` 可选参数，None 走旧路径），**非重叠 2.7× / collect_all 3.3×**；④ `_eval_feature` 一趟 partition + median hoist + `_auc_within_day` valfn 预取，1.164×；⑤ `_bear_regime_stats` observed 列表去重计算，1.05×。新增等价钉测三文件（`test_ohlc_hoist_equivalence`/`test_scan_signals_ytd_equivalence`/`test_mainline_fingerprint_equivalence`，新旧实现逐位 `==`/stdout 逐字节）。⚠️ `simulate_b1_trade` 因 ohlc 分支 C18→C19（既有函数 +1，换 2.7-3.3× 提速）。**未实施的性能机会**（记入 TODO #59）：scorer 每 bar 全前缀重算（evaluate_trades 基准中占 50%+，需各 scorer 改双形态）；RSI 两 gate 的 `precomputed` 形参未接线（~10-20× 潜力）；pit_financials 逐期全量台账读写（~97% IO，但改故障恢复语义）；东财分页并发（改限流特征，不安全） | C19-20 是可维护性观察名单的最后一批；性能侧沿用 v0.68「逐位等价才动」的纪律 | 全量 4183 passed（+36 新钉测）；audit 套件通过（ruff format ✓ / mypy 0 errors）；radon 重生成确认 29 个目标清零；周报/月报 md+json 四件逐字节 MATCH |
| 2026-08-19 | v0.74 | **#59 性能机会落地（①②完成，③待拍板）——回测热路径 O(n²)→O(n)，全部逐位等价 + 钉测**。① **scorer 双形态框架**：`_SCORER_PRECOMPUTE` 按 scorer 身份注册逐股预计算（查不到 ⇒ None ⇒ 旧路径，未接线 scorer 零风险），`_entry_signal` 传第三参、`_dual_form_scorer` 兼容两参外部 scorer；接线三个——`_sc_b1_pullback`（MA5/10/60 rolling + J 全序列预计算，尾部 ≤45 根窗口统计仍每 bar 现算；n<60 边界按旧表达式兜底）合成基准 **3.3×**、`_sc_kdj_j` **10.0×**、`_sc_rsi_state`（`rsi_state_score`/`rsi_multi` 加 `rsi_series_map`，5 次 RSI 递归消成 3 序列预计算）**6.3×**。② **RSI 两 gate 的 `precomputed` 形参接线**（v0.68 留了形参没人传）：`rsi_regime`/`rsi_divergence` 加 `rsi_series=None` 双形态，`_precompute_gate_series` 增 `rsi14` 键，rsi_strong/rsi_bull_div 3.2-3.3×、组合 gate（J<13 短路 + 双预计算）**10.3-11.0×**。等价依据同 v0.68：Wilder/EWM/rolling 均从第 0 根递归，前缀末点==全序列同位点（同一串浮点运算）；`evaluate()` 的 gate_window>0 路径起点非零不适用、未动。钉测：`test_gate_precompute_equivalence` 106→119 例、新建 `test_scorer_precompute_equivalence` 54 例（逐 bar dict `==`/逐笔 `==`/防空转/两参外部 scorer 回归）。其余 scorer 评估：s_shape 系值得但改造面大（MA50/60+多分项，待排）；alpha101/pvcorr/low_vol/momentum/reversal_quality/mcap 中等优先；b1_dual/long_structure/b2/main_rally 为黑盒 detector 不适合；baseline 不需要。**③ pit_financials 逐期全量台账读写（~97% IO 可省）未动**——逐期落盘是刻意容错语义且影响 stdout 数值，留 TODO #59 待 owner 拍板口径 | 回测耗时是研究迭代最大摩擦点（v0.68 起同一动机）；「逐位等价才动」纪律延续 | 全量 4249 passed；audit 套件通过（ruff format ✓ / mypy 0 errors）；基准脚本均 `assert 新旧输出 ==` 后置 /tmp 未入库 |
| 2026-08-19 | v0.75 | **#32 对账 replay_failed 根因修复：股份入账类（转债转入/拆股）参与持仓推导**。目标机观察期首日即 replay_failed，分叉点：000938「2022-06-06 卖 3 股、当时持仓 0」——台账里**有**股份来源（2022-05-13 转债转入 3 股 @17.93），但 `compute_positions`/回放只认 `TRADE_CATEGORIES={"买入","卖出"}`，转入被过滤 ⇒ 回放只见卖出 ⇒ 假超卖。修复：`incremental_ledger` 新增 `SHARE_CREDIT_CATEGORIES={"转债转入","拆股"}`（台账实测形态，口径参照 backtest_0amv_bear_regime 的入库规则：603606 拆股 +600、159938 零成本转债转入 +22500），`compute_positions` 对其按买入同构累加——成本按记录价格加权（转股价=可得最优成本近似）、零成本/拆股摊薄单位成本；`reconcile_positions.replay_ledger` 类别过滤同步。live 增量导入同受益（`_commit` 未过滤直传 `compute_positions`，两类缺陷同根修复）。**刻意不改**：周/月报 FIFO 与 `trades_stock.json` 仍只认真实买卖（有意口径，weekly_review.parse_ledger docstring + test_calc_mfe_mae 钉住）； baseline 通道保留（台账非零起点的合法情形仍在）。⚠️ 行为变化仅限：含入账类记录的台账回放/增量导入结果（此前漏算股份来源） | #32 观察期发现：「可检测」机制跑起来第一天就抓到真口径缺陷——这正是观察期的意义 | 新钉测 4 例（转债转入成本/拆股摊薄/回放认入账类/银行转证券等仍忽略）；全量测试与静态检查不回归；目标机拉取后重跑对账应转 ok，观察期自该日起算 |
| 2026-08-19 | v0.76 | **#32 成本差异定性：券商摊薄/保本成本 vs 回放移动平均成本，口径差非缺陷**。v0.75 修复后目标机重跑：`replay_ok=true`、数量 6 对 6 零误差，4 只 cost_only_diff 逐笔核算——920808 摊薄 78.696 vs actual 78.667（部分止盈摊低，残差 0.037%）、600150 按回合摊薄（06-09 清仓重置）35.50 vs actual 35.514（部分止损摊高，残差 0.03%）；两只同向残差 ~0.03% = 券商保本价含预估卖出佣金量级；另 2 只为快照 3 位小数舍入（~1e-5）。定性写入 reconcile_positions docstring 新增的「成本差异的第三类」节：**硬信号只有数量**（--strict 只看 qty_mismatch_count），cost_only_diff 不构成告警；成本口径统一（若要）是独立课题。附带发现台账存在 `除权除息` 类别（现金分红 0.37/零值行），数量 0 不进持仓计算，天然免疫 | 观察期数据定性，避免把口径差当缺陷追查或误调 cost-tol | 观察期继续：数量持续零 mismatch 后 --strict 可转硬闸（成本不阻塞） |
| 2026-08-19 | v0.77 | **#26 板块信息利用第一步（owner 定案口径）**：每日记录涨幅 top10 板块（含涨停家数=资金流入代理 + 主力净流入）与跌幅 top10（含跌停家数），1800 池（J<13）判命中「近期多次上榜=活跃」的板块。落地两件：① `pipeline/market_timing/sector_daily_rank.py`——单日/回填双模式，板块宇宙=sector_index CSV ∩ 名称表 tdx_type∈{2行业,4概念}（881 细分留 `--include-types`）；涨停家数自算（TDX 无分板块现成统计，880006 只有全市场口径）：未复权价按 `price_limit_pct`（ST 主板 5% 补充层），每股算一次再按 `invert_members`（1800 同口径）聚合；资金流按板块名聚合 `{date}_fund_flow_rank.json`（缺失降级 null）；活跃名单=近 40 榜日上榜 ≥2 次（owner 口径，参数可调）落 `sector_active.json`。② `research/sector_inflow_study.py`——离线验证「活跃板块 ∩ J<13 池」：逐日 as-of（j_series 全序列一次算按索引取、滑窗增量计数，无未来函数）× forward H5/10/20（qfq 与回测/live 同口径），命中 vs 未命中 vs 全池六统计量 + 前后两半跨窗稳定性（R10 教训）。**纪律**：先证据层后加权（板块腿两次证据不稳的前车：R4 未复现、v0.50 移出总分），命中有效才谈进 1800 证据列 | owner：「记录每天涨幅最大的10个板块…判断哪些是命中之前有过资金流入的板块」；历史全部本地可回算，验证不用等积累 | 合成测试 26 例；全量 4286 passed；audit 通过；真实跑数在目标机（回填 + study），结论回来后决定 1800 接线 |
| 2026-08-19 | v0.78 | **核心思想落档（owner 定案，记入 `strategy/_shared/system_principles.md` 核心原则第 0 条）**：「不追求胜率，追求一定胜率基础上的更高盈亏比，通过优化止盈止损和仓位管理达成盈利」。三条推论一并写入：评价信号看期望/盈亏比不把胜率当目标；改进杠杆优先加在出场与仓位而非入场精度；与既有研究口径一致（R2 选股无 alpha，出场侧是少数反复过线的地方）。对当前工作的直接指引：#26 等信号类验证以期望/盈亏比与跨窗稳定性为判据（胜率仅作底线参考），优化 effort 优先投向 m2 出场/仓位方向 | owner：「这个是这个项目的核心思想」——落档防漂移 | 后续研究/复盘报告的评价指标按此口径解读 |
| 2026-08-19 | v0.79 | **#26「活跃板块 ∩ J<13」证伪关闭（三轮研究，预设判读协议执行到底）**。假设（owner）：1800 池（J<13）里命中「近期多次上榜=资金流入」板块的股票后续更优。R1（120 窗无过滤）：三 horizon 全跨窗翻转，不过线；R2（regime 变体，--amv-regimes 做多,中性）：120 窗三 horizon 全正且两半同向（H20 +2.52pp），单窗「过线」；R3 稳健性（240 错位窗，77/240 日入样）：**三 horizon 全部前后半翻转**（H20：前半 −0.57pp / 后半 +2.52pp）——后半精确复现 R2 数字（+0.42/+1.15/+2.52，机器可复现），前半 2025 下半年强反弹期反向（未命中组均值全正）。机理：弱市强势板块抗跌、强反弹市滞涨，同一枚硬币两面，edge 随 regime 反转合计抵消——R10/R4 同款「单 regime 假象」。**产物处置**：`sector_daily_rank` 采集器保留（#26 回调四项切换检查的原料）；`sector_inflow_study` 留档可复跑；「活跃板块命中」假设入 TODO 已失效表，**不接 1800** | 预注册判读协议的价值：R2 若没有 240 窗复核就会被当成「跨窗稳定」接进 live——半分一致只是同一 regime 内的假象 | 已失效表落档防重跑；#26 转「回调四项切换检查」子项 |
| 2026-08-19 | v0.80 | **主题/板块逻辑清理第一批（owner 四项拍板，全盘点后执行）**：① **1800「当日主线指纹」节删除**——候选池板块族密度榜整体移除（candidate_table 节 + sector_mainstream 的 `mainline_fingerprint` 及 helper/`sector_sizes`/三张剔除表；`invert_members`/`sector_name` 等被 enrich/daily_rank 使用的部分保留）；连带 `sector_mainstream` FACTOR 改 `live_use=none`/`stage=debug`（live 引用已消失，研究侧 `aggregate` 保留）。② **cz_avoid_sector 硬否决删除**（owner 拍板）——全仓唯一真实板块否决（avoid→封 D）下线：`CZ_SECTOR_PREFERENCE.json` 删文件、registry `cap_rules.cz_avoid_sector` 删键、`cz_sector`/`cz_sector_status` 落盘字段与 `cz_sector_preference_missing` 降级分支移除、STRATEGY_REGISTRY/cz README/SCREENING_WORKFLOW 同步。⚠️ 行为变更：原黑名单板块票不再封 D。③ **资金意图板块腿删除**（owner 要求评估后判定无正向依据）——`fund_flow_inflow` 触发条件去掉 `sector_inflow_positive` OR 分支，只认个股在榜且净流入；理由：板块净流入是成员加总（蹭标签拿分）、归属来自已不可靠的主题映射、同源假设 v0.79 已证伪。⚠️ 行为变更：原靠板块净流入拿 +2 的票资金意图降分。其余展示层（板块列/共振腿/theme_tracker/sector_state 契约链/sector_daily_rank）**保留不动**（owner：后面遇到再做判断；契约链保留） | 板块证据两次不稳（R4/v0.50）+ 一次证伪（v0.79）后的瘦身；纯展示与有真实行为的两类分开处理，行为变更显式落档 | 全量 4278 passed；audit 通过；新增钉测「仅板块腿不再计分」；分层分布变化（cz avoid 解封 + 资金意图降分）观察若干交易日 |
| 2026-08-19 | v0.81 | **止盈止损规则集中化（因子×止盈×止损架构 Phase A，owner 批准计划）**：新建 `core/exit_rules.py`（L0，**live 唯一来源**，仿 b1_thresholds 模式）——止损方案目录 `hard_loss`（−10%）/`loss_reduction`（−7%）/`breakeven_stop`/`trailing_stop`/`time_stop`，止盈方案目录 `scale_out_two_bull`（BBI 上方连续两根中大阳分批止盈）/`cost_zone_flat`（成本区「不涨就拍」）；每方案 `{rule_id, enabled, params}`，**默认 enabled/params == 当前 live 实际行为**（后四个仅研究侧 backtest_factors 在跑 ⇒ enabled=False，params 键名与 `simulate_b1_trade` 形参一致，即 Phase E 研究→live 回流 schema）；减仓幅度表（P0 全清/P1 减 10-25%/P2 减 10-20%）同源收进。三份硬编码拷贝（`b1_holding_state._pnl_signals`、`review_core._hard_risk_signal`、`portfolio_review_report.classify`）改从 exit_rules 导入并接 enabled 开关，默认值不变 ⇒ 输出逐字节不变；止损理由文案随阈值动态拼（防「配置一改就谎报」，同 b1_thresholds 教训）。新建 `governance/contracts/EXIT_RULES.json` 覆盖层（仿 `resolve_cap_rules`：未知方案/参数键忽略、默认表兜底、模块导入时求值），内容与代码默认值一致。⚠️ **唯一有意行为修正**：`portfolio_review_report` 的 J 低位阈值 12 → `b1_thresholds.J_LOW_THRESHOLD`（13）——此前该报告对 J∈[12,13) 的票**漏出**「J值低仅作观察」理由（全仓其余判定点 13 口径会出）；修正后该区间多一条理由行（只加理由，不改动作/优先级）。刻意不并入：`weekly_review`/`cooldowns` 的 −7% 是**已实现亏损**的止损合规复盘口径（百分数单位 −7.0），语义不同 | −10%/−7% 有三份硬编码拷贝、J 阈值 12/13 不一致、保本/移动/成本区/时间止损只在研究侧——与反转 K 阈值 2026-08-07（v0.36）收敛是同一类分散问题 | 默认值==现网口径/三处同源/JSON 一致/未知键忽略由 `tests/test_exit_rules.py` 钉住；分层守卫登记 L0；全量测试与静态检查不回归；J∈[12,13) 持仓票的日报新增一条 J 低观察理由行（预期内） |
| 2026-08-19 | v0.82 | **持仓计划持久化（因子×止盈×止损架构 Phase B）**：新建 `core/trades/position_plans.py`（L2）——买入成交导入时把该持仓的止损/止盈计划落盘 `data/trades/position_plans.json`（`{positions: {代码: 计划}, archive: {代码: [历史]}}`，temp+os.replace 原子写），补上「选股 stop_loss_ref 买入后丢失」的断裂链。计划条目 `{entry_date, entry_price, stop{rule_id,price,basis}, take_profit{enabled 方案参数快照}, source, created_at, rules_version(配置短哈希)}`；止损价来源：≤ 买入日最近一份 `stock_pool` 中该代码的 `stop_loss_ref.price`（`source=candidate:{date}`，basis 随带）⇒ 找不到兜底 `entry×(1+loss_reduction −7%)`（`source=default`，basis 注明兜底口径）。同步规则：新建仓（本批有「买入」行的新代码）生成；补仓不重建、**止损价不动**、`entry_price` 随摊薄单位成本更新并标 `averaged: true`（选摊薄成本而非该笔成交价——live 三处判定点的 pnl 口径是（现价−单位成本）/单位成本，计划基准必须与判定基准同源）；清仓移入 archive（带 `closed_at`）；转债转入/拆股（SHARE_CREDIT）不触发。写入点挂 `incremental_ledger` 两处：`apply_positions`（直接调用方路径）与 `main` 的 `_commit` 之后（刻意不进 staged——计划是派生数据可从台账+候选池重建，crash 窗口内落后一拍可检测可修复）。⚠️ 全量灾备导入 `standardize_trades` **不经过** apply_positions（直接覆写 current_positions.json）⇒ 不重建计划，已在代码注明（无逐笔成交上下文，重建后需人工复核）。**本 Phase 只落盘**：positions/台账/报告判定与渲染零变化（消费方是 Phase C 影子判定）。conftest 全局把 PLANS_FILE/POOL_DIR 改道 tmp（防测试写真实 data/） | 选股侧 stop_loss_ref 买入后即丢失，持仓侧只能按通用 −7%/−10% 现算——计划点位落盘是 Phase C 影子双跑的前置 | `tests/test_position_plans.py` 7 例钉住（candidate 来源/兜底/补仓不覆盖/清仓归档/入账类不触发/坏 JSON 兜底/apply_positions 挂钩）；全量测试与静态检查不回归；positions/台账/报告输出零变化 |
| 2026-08-20 | v0.83 | **14:45/17:00 持仓计划影子消费（因子×止盈×止损架构 Phase C，owner 拍板影子模式先行）**：`b1_holding_state.evaluate()` 加可选参 `plan=None`——有计划时产出影子信号：现价 ≤ `plan.stop.price` ⇒ `plan_stop_breach`（P0 级影子，理由带 rule_id/basis/两价位）；`plan.take_profit` 快照里 enabled 的 `scale_out_two_bull` 复用现行「BBI 上方连续两根中大阳」量价证据判定（`require_above_bbi` 参数生效），命中 ⇒ `plan_tp_scale_out`（P2 级影子）。影子结果写入返回 dict 新键 `shadow`（`{reason, plan_source, plan_based_priority, plan_based_action, signals}`）——**不进 SIGNAL_ORDER、不进 final_priority/bucket/权限/任何既有字段**（影子期铁律，钉测「同输入带/不带 plan final 完全一致」）。无计划（存量持仓全部早于 position_plans 机制落地，影子期常态）如实标 `plan_missing`，不报错。`review_core`（14:45）新增「§2.1 持仓计划影子对比」节、`final_close_review`（17:00）§5 审计行后新增影子对比列表：计划判定 vs 现行判定，优先级不一致行内标「⚠️影子不一致」，无计划标「无计划（早于机制落地）」；行格式单源收在 `b1_holding_state.shadow_compare_line`（两份报告共用）。两报告 main 各经 `load_plans()` 读 `data/trades/position_plans.json` 的 positions 节传入 evaluate（conftest 全局改道 tmp，测试不碰真实 data/）。观察期判据入 TODO #60：连续 5 交易日影子对比后 owner 拍板是否并入 SIGNAL_ORDER，切换是独立提交 | 计划点位已落盘（v0.82）但无人消费——影子双跑是「计划信号转正式判定」前的证据积累期 | `tests/test_position_plans_shadow.py` 13 例钉住（final 一致性/破位影子信号/止盈命中与量价陈旧门槛/plan_missing 不炸/坏形状兜底/⚠️影子不一致渲染）；全量测试与静态检查不回归；现行判定输出零变化 |
| 2026-08-20 | v0.84 | **因子化 + 权重外置（因子×止盈×止损架构 Phase D，owner 拍板本轮行为零变化）**。① **资金意图因子化**：`score_candidates.capital_intent_strength` 及 `_capital_intent_*_evidence` 段落函数迁入 `core/factors/capital_intent.py`（FACTOR：live_use=scorer、stage=release——它实际驱动分层第二轴）；score_candidates 改 import 调用（`sc.capital_intent_strength` 同一函数对象）。**status 定档 candidate**：NOT_FOR_LIVE={needs_work,untested} 由注册表测试机械禁止与 scorer 共存，active 语义「已验证可用」需要回测证据而本轮只是搬迁 ⇒ 取 candidate，分值校准挂回测 TODO（Phase E 寻优入口）。② **基本面因子化**：新建 `core/factors/fundamentals.py`（FACTOR：live_use=evidence_only、stage=release、status=untested，先例 rsi_state）——`financial_factor` 纯评估实现自 `screening/financials.py`、`fundamental_quality` 品质档自 score_candidates 上迁（⚠️ 方向只能是「实现上迁、原模块 re-export」：factors/ 是 L2 不得依赖 L3 screening，架构分层测试强制）；只进 🐂 展示与四面共振基本面腿，不进分，行为不变。③ **权重外置**：registry 增 `scoring.weights` 节——技术分八段分值 + `TECH_STRONG_FALLBACK=60/TECH_MID_FALLBACK=30`（score_candidates `DEFAULT_TECH_WEIGHTS`/`resolve_tech_weights`）与资金意图证据分值 + `CAP_STRONG=5/CAP_MID=2`（capital_intent `DEFAULT_EVIDENCE_WEIGHTS`/`resolve_capital_weights`，ci_* 前缀区分技术分同名证据）改从 registry 读，覆盖模式仿 resolve_cap_rules（未知键忽略、默认兜底），**默认值==现值**。④ 情绪维度缺口显式标注：无数据源未建因子（factors/__init__.py 维度覆盖节 + SCREENING_WORKFLOW 可配置项节）。⑤ 注册表钉板随定档同步：release 集合 11→13、evidence_only 集合 +fundamentals | 因子维度偏科、权重硬编码——因子化让因子实现全项目唯一一份、可单独回测；权重外置让分值可调而不改代码。行为零变化是 owner 拍板底线：迁入≠验证，是否进分由后续回测证据决定 | `tests/test_scoring_weights.py` 8 例（同一函数对象/registry 与默认表逐键一致/未知键忽略/覆盖真实生效）；**行为零变化硬验收**：2026-08-19 stock_pool.json + candidate_table.md 改动前后重跑，屏蔽 report_id/策略版本/数据截止/输入 sha1 后**逐字节 MATCH**（基线已恢复原文件）；全量测试与静态检查不回归 |
| 2026-08-20 | v0.85 | **回测联合寻优驱动器（因子×止盈×止损架构 Phase E）**：新建 `research/strategy_grid.py`——网格 = 因子轴 {scorer × entry_gate} × 出场轴 {stop_mode/stop_pct/trail/breakeven/scale_out/cost_zone/time_stop}，每格一次 `backtest_factors --trade-sim --portfolio` 子进程（串行驱动模式复用 m2；`_margin`/`_breakeven_wr`/`_ret_over_dd`/`_load`/`SCRIPT` 直接 import，不复制）。复用机制提升为 `cell_signature`——完整 CLI 参数的 sha1 短哈希进结果文件名，签名一致即 [SKIP] 复用（比 m2 指纹更保守：组合层参数也在内，且不需要先解析 universe）。出场参数键名与 `simulate_b1_trade` 形参 / `EXIT_RULES.json` params **三者一致**，每组结果附带 `exit_rules` 块（rule_id/enabled/params；stop_mode/stop_pct/scale_out_frac 等研究侧独有参数进 `research_only` 注记，不伪造 rule_id）可直接拷入 live 配置——研究→live 回流通道落地。目标函数 = margin + expectancy_R + 0.05×(return/maxdd)（`--obj-weights` 可调；三项量纲不同，文档明示是排序启发式）。剪枝：`--max-runs` 子进程预算（复用不占额，超出按序截断并在报告注明）+ 两阶段（粗网格 = 因子轴 × 出场轴基准档 → `--top-k` 个因子组合细化全出场档，基准档经签名复用不重复入榜）。报告头部固定标注 **R11 未决**（基准已实现口径为负 ⇒ 读数仅供相对排序、不得引用绝对量级）。CLI 默认网格小（2 scorer × 2 gate × 5 出场档），`--scorers/--gates/--exit-grid` 可扩；`--date/--universe/--codes-file/--start/--end/--count/--cost-bps/--top-n` 透传 backtest_factors。**本轮不做**：live 技术分 scorer 的回测侧复刻；实跑寻优 campaign（R11 先行） | 无因子×出场联合寻优 ⇒ 因子搭配与止盈止损只能分开单变量扫，而 m2 已证明单变量最优 ≠ 组合最优（trail_08 × tick_buffer_3 正交互补是手工试出来的） | `tests/test_strategy_grid.py` 12 例钉住（笛卡尔积展开/签名一致复用跳过/输出 JSON schema 键名/exit_rules 块 ⊆ 真实 EXIT_RULES rule_id/--max-runs 截断/两阶段 top-k/margin 口径）；mock subprocess 不跑真回测；全量测试与静态检查不回归 |
| 2026-08-20 | v0.86 | **v0.81-v0.85 三轮 review 修复批（无 blocker，1 major + 9 minor 全修）**：① **strategy_grid cell_signature 补钉窗口/宇宙**（major，m2 文档化三次的「隔天续跑混口径」事故类）——`_pin_context` 把隐式滚动窗口固化成显式 start/end 进签名（参照票 600000 日历换算，失败降级 unpinned+警告）、宇宙 codes digest 混入签名（--dump-codes 探针）、报告 data_quality 块 + schema v2；顺带：expand_grid/_cells_for 双实现收敛、ret_over_dd 缺失排尾（原按 0 压在真实负值上）、残缺输出计 failed 并删毒文件、research_only 优胜配置加「live 无法表达」提示、--date+--end 同给 WARN、子进程加 --timeout（默认 1800s）、exit name 文件名 sanitize、复用 [SKIP] 打印。② position_plans：池文件顶层非 dict 按损坏跳过（原会在 _commit 成功后 AttributeError、计划永久落后）；sync_plans 两调用点异常隔离（stderr WARN，非静默吞错）；closed_at 改取本批最后卖出日；「可检测可修复」过度声明改如实口径；丢失后补建标 rebuilt 与真摊薄 averaged 区分。③ 权重外置：覆盖值类型校验（非数值忽略+WARN 去重，防 TypeError 违背「绝不 raise」）；**生效权重落盘**（stock_pool 结果壳末尾新键 weights，契约钉板兼容确认后加键——修复「registry 改权重无留痕」审计缺口）；v0.84 行测试例数 9→8 事实修正。④ nit：影子文案「跌破」改「触及/跌破」（判定含等号）、final_close_review 影子行 name None 兜底、test_release_set_is_the_known_twelve 改名 thirteen。⑤ review 附带发现：`rsi_multi` 因 #59 双形态分支升到 D21——拆 `_resolve_rsi_series`/`_prev_or_none` 压回 C16，D 级清零恢复；`bottom_patterns` FACTOR 元数据补记（volume_yy 实为技术分打分腿，注册表粒度是模块级，见 TODO #62） | review 的价值：major 是实跑寻优前必须堵的静默混口径；「绝不 raise」「审计留痕」是仓内既定纪律 | 新增/更新钉测 ~40 例（签名漂移不复用/残缺计 failed/类型校验忽略+WARN/权重落盘/异常隔离仍导入成功等）；全量 4372 passed；audit 通过 |
| 2026-08-20 | v0.87 | **#62 技术分轴因子化收口（owner 拍板「该因子化的都因子化」，三批搬迁全零行为变化）**：enrich 内联检测器全族迁入 `core/factors/` 并 FACTOR 注册——批 A `macd_technics`（7 打分腿+三打白骨精 cap，最大缺口）；批 B `volume_detectors`（bottom_volume/leader_volume/volume_sustain）、`b1_structure`（five_day_entry/repair_signals/non_one_wave/liquidity/_stop_ref）、`weekly_j`（weekly_j_state/j_below_threshold）；批 C `ignition`（check_ignition/check_pullback_shrink/b1_ignition 复合抽纯判定）、`entry_patterns`（patterns 五单项判定，j_low +24 最大单项）、`j_low_gate`（J<13 进池硬门槛登记，撞名避让研究侧 as-of 适配器 `j_low_gate` ⇒ 入口名 `j_low_gate_hit`）。定档一律 status=candidate（驱动打分但无独立回测证据，NOT_FOR_LIVE 机械约束）、stage=release；live_use 按实际消费分 scorer/gate。enrich 侧全部保名 re-export（monkeypatch 通道不破，conftest env-reload 链同步插新模块）；判定逻辑自此**可被 backtest_factors 单独引用回测**（#61 校准的前置）。刻意不迁：技术分组合器/RESONANCE_MATRIX/cap 编排留 score_candidates（它本身就是 scorer 的家，权重已外置）；check_three_lows 等非打分项留 enrich。等价验证：各批 git show HEAD 旧版 vs 新版合成数据输出 dict 逐位 ==（批 C 含完整 compute_metrics 逐字段 + 42 组 J gate 组合） | 「因子实现全项目唯一一份」的硬约束此前只管指标层，判定层散落 enrich——技术分轴收敛后，调任何判定只有一处可改 | 全量 4400 passed；audit 通过（ruff format ✓ / mypy 0 errors / radon 无 D 以上）；注册表钉板随批次更新（release 11→20、INLINE_EXTRACTED 4→11） |
| 2026-08-20 | v0.88 | **因子·止损·止盈总览落档**：新建 `strategy/_shared/factor_exit_catalog.md`——注册表全量 31 因子按角色三组（live 打分/门槛 10 个含各自喂的打分腿与分值、证据层 10 个、研究调试 11 个）+ 止损方案目录（hard_loss −10%/loss_reduction −7% live，保本/移动/时间止损研究侧）+ 减仓幅度表 + 止盈方案目录（scale_out_two_bull live、cost_zone_flat 研究侧）+ 研究→live 回流通道说明。头部声明「人读索引，字段级真相以代码为准」（factors 注册表 / exit_rules.py / EXIT_RULES.json / scoring.weights）；_shared/README 索引同步。纯文档，零代码变化 | 三件套散落在注册表/契约/代码注释里，没有一份人读全景——核心思想（盈亏比靠出场与仓位）要求出场侧与因子同等待遇，先要有这份地图 | 文档与代码一致性抽查（注册表 31 项、EXIT_RULES 目录、权重分值均取自运行时代码输出）；C 组 11 个非 live 因子同日复核后展开为明细表（定义/status/降级原因与证据出处逐行） |
| 2026-08-20 | v0.89 | **候选表「门槛外观察区」改为「门内提醒」（owner 指示）**：被 J<13 硬门槛挡掉的票不再单列展示——`watchlist_outside_gate` 机制整体移除（enrich 不再产出、score_candidates 不再透传、stock_pool 契约删可选键），门外票只进 `excluded` 留痕。替代节「📌 门内提醒（J≤13 且异动强）」列**池内**票：日 J≤13 且异动强（判据沿用：底部巨量 `bottom_volume.hit` 或放量点火 `ignition.hit`），按技术分降序最多前 20 只（超出注明总数）——仅提醒，不改分层/排序。⚠️ stock_pool 输出键变化：删 `watchlist_outside_gate`（原恒为数组，无其他消费者） | owner 复核：门外观察区（J≥13）价值低，要的是池内异动票的提醒；判据不变只换作用域 | 新钉测 4 例（节内只列 J≤13/空节不静默/按技术分降序截断）+ 门槛行为钉测改写 3 例（被挡只进 excluded）；全量测试与静态检查不回归 |

# R22 · 打分重构：让 TOP20% 赢家浮现（score_variants_study，V0-V3 双窗）

> **家族**：选股方向（打分重构，R2 关闭后的第一次「按证据重开」尝试）　|
> **证据等级**：L3（双窗对照 + 判据预注册；vipdoc 宇宙带幸存者偏差）　|
> **状态**：✅ 方向翻正（V2/V3 双窗四判据 C1/C2/C4 全过）；⚠️ 预注册 C3（盈亏比
> ≥V0）差一线，**owner 2026-08-26 拍板放宽线（盈亏比 ≥2.4 绝对下限）后双窗通过**
> ——放宽候选，进 live 前仍须独立窗口复核
> **依赖**：上游：R19（现行分区分度反向）R20（逐腿 lift 证据）R21（深水 gate 跨窗）
> ｜判据：R12（预注册纪律）｜口径：R11（量级不作数）R14（幸存者偏差）
> 索引与主图见 [`README.md`](README.md)。产物：`artifacts/logs/score_variants_study/`
> （`s0_n400` 主窗 / `s0_n1000_cw` 跨窗 + 两个 `.rejudged.json` 放宽线重判）；
> 脚本 `research/score_variants_study.py`（V0 重建与 live 逐位一致有钉测）。

## 主题

R19 证实现行技术分把高分给了输家（TOP20% 赢家均分 36.6 < 对照 40.2）——
能否按 R20 的逐腿证据重构打分，让真正优秀的股票（TOP20% 赢家）得高分。

## 目标

owner 目标：「保持高盈亏比的同时尽量提升胜率，让 TOP20% 浮现」。
预注册判据：C1 Spearman>0 且半窗同正；C2 赢家均分 > 对照；C3 变体 top20%
篮子胜率 > V0 且盈亏比 ≥ V0；C4 跨窗符号保持。

## 结论

**① 证据重构（V2）方向完全翻正且跨窗放大。** V2 = 只留 R20 正向腿：
rsi_deep 40 / weekly_j_low 20 / rsi_bull_div 20 / macd_bottom_divergence 20
（简单整数，可回流 scoring.weights 形态）。Spearman 主窗 +0.091 → 跨窗
**+0.136**（半窗同正）；赢家均分 16.1 vs 对照 9.4（主窗）、19.8 vs 9.6（跨窗）。
**V2 高分档（≥60）是真金**：主窗 769 笔均收 +7.90%、胜率 57%、盈亏比 3.26。
V3（V2 + 反向腿每条 −5）略优但复杂度换的，差异在噪声内。

**② 篮子胜率翻倍、盈亏比小幅回落。** 变体选出的 top20% 篮子：胜率主窗
27.2%→46.5%、跨窗 28.8%→53.7%；盈亏比 2.81→2.41（主窗）/2.89→2.78（跨窗）；
篮子均收 +0.16%→+3.25% / +0.46%→+5.69%。预注册 C3 判淘汰（盈亏比降了）；
owner 放宽线（胜率显著升 + 盈亏比 ≥2.4 绝对下限）下 V2/V3 双窗通过。
⚠️ 放宽是事后判据，已并列标注（`candidate` vs `candidate_relaxed` 两列并存）。

**③ V1（反向腿取反）证伪。** 主窗 Spearman +0.031 看似翻正，跨窗 −0.001
且半窗翻转——取反不是证据，机械变号把「少输」错当「会赢」。

**④ 下一步**：~~V2/V3 进 strategy_grid 因子轴~~ **已否决**——独立窗
（2010-2019，s1000，14068 笔）复核翻车：Spearman 萎缩到 +0.04 且半窗翻转
（−0.018/+0.090），篮子盈亏比 1.97/2.00 破放宽线。教训入库：R20 的富集画像
（2024-2026 强 regime）推到打分层后仍是 regime 依赖的——**画像稳 ≠ 交易稳 ≠
跨 regime 稳**，三层证据各管一层。live 打分维持现行（V0），权重调整一律回
strategy_grid 跨 regime 三窗验证后再议。

---

### 天然基准重判（v0.128，owner 2026-08-28 拍板判据口径）

「胜率/盈亏比的判线不拍脑袋」⇒ C3 增加**零自由参数的天然基准口径**
`C3_natural_margin`：篮子 margin（胜率 − 盈亏平衡胜率 1/(1+盈亏比)，m2 同口径）
> V0 篮子 margin 即过线，显著性用 Wilson 95% 区间是否重叠标注。
预注册 C3（≥V0）/放宽 C3（≥2.4）/天然 C3 三列并列、互不覆盖。

三窗篮子 margin（pp）：主窗 V0 +1.0 / V2 +17.2 / V3 +17.8；跨窗 V0 +3.1 /
V2 +27.2 / V3 +27.6（均 Wilson 显著）；pre2019 V0 +1.8 / V2 +5.7 / V3 +8.3。
⇒ **天然口径下 V2/V3 主窗跨窗都是候选，但 pre2019 的 C1 半窗翻转不变**
（margin 只替代 C3，其他判据原样）——三窗终审维持否决，结论不变但判据从此
无自由参数。

## 证据与过程

口径：固定基底（0AMV做多 ∧ J<13）× 出场 = stop12 + 保本0.05 + 分批止盈0.5 +
BBI 连破 2 根（= v0.118 臂）；一轮采集四个变体离线求值（panel_hook 同时产
factor_contrib + 29 键 panel，V0 重建与 live 逐位一致钉测）。主窗 400 只
seed=0 全历史 15636 笔；跨窗 2022-2024 s1000 14687 笔。判据预注册写死在
代码与报告头部；放宽线重判走 `--from-trades` 离线复算（不重跑回测）。

### ⚠️ 边界

- **R11**：量级不作数，只用于方向与相对排序；**R14**：幸存者偏差宇宙。
- 变体是「按 evidence 手工设计」的 4 个方案，不是网格搜索——多比较偏差有限，
  但 V2 权重（40/20/20/20）是拍的，未做灵敏度分析（下一轮 strategy_grid 扫）。
- 放宽候选 ≠ live 就绪：独立窗口复核 + live 接线拍板是两道独立闸。
